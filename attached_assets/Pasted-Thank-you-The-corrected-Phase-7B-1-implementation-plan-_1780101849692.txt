Thank you. The corrected Phase 7B-1 implementation plan is approved.

Approved scope for 7B-1 only:

* install frontend packages:
    * docx
    * xlsx
* lazy import setup
* file-generation sanitizer
* DOCX generation
* XLSX generation
* reusable OraExportMenu
* integrate export menu into:
    * ora-message-actions.tsx
    * ora-panel.tsx
    * ora-bubble.tsx
* keep CSV / JSON / Markdown export working
* no Raw Dataset sheet by default
* no PPTX yet
* no PDF / Print yet
* no reportMode AI prompt changes yet
* no ReportResult endpoint
* no OpenAPI changes
* no backend route
* no DB changes
* no Knowledge Vault
* no image generation
* no web search

Important implementation requirements:

1. Lazy imports are mandatory

docx and xlsx must only load when the user clicks the export/generate option.

Do not include them in the initial landing-page bundle.

2. Sanitizer must run before generation

Before generating DOCX or XLSX, sanitize the content and remove:

* fileRef
* imageRef
* datasetRef
* sessionToken
* handoffToken
* handoffCta
* editedFrom
* hadAttachment
* suggestions
* apiKey
* authToken
* secret
* projectId
* builderId
* containerId
* neonProjectId
* flyMachineId
* base64-like long strings

3. Raw Dataset must be off by default

Do not include raw dataset rows in the Excel workbook unless a future approved opt-in feature is added.

Default Excel sheets should be safe summary sheets only.

4. Export UI

Use one clean dropdown:

“Export / Generate”

Options may include:

* Word Report
* Excel Workbook
* Markdown
* JSON
* CSV Action Plan when available

Do not clutter the header with too many icons.

5. No backend work

7B-1 must stay frontend/client-side only.

Do not add:

* API route
* DB table
* OpenAPI field
* reportMode
* server file storage
* server export generation

6. Validation required after implementation

Please provide a full validation report with:

* affected files
* final commit hash
* proof docx and xlsx are lazy-imported
* proof initial bundle is not bloated by these packages
* proof DOCX export works
* proof XLSX export works
* proof Markdown export still works
* proof JSON export still works
* proof CSV action plan export still works
* proof generated files do not include banned fields
* proof no Raw Dataset sheet is included by default
* proof object URLs are revoked
* proof no file content is logged
* proof no backend route was added
* proof no DB/OpenAPI changes
* typecheck/lint/format status
* workflow status
* rollback plan

7. Manual open validation required

Please confirm generated files open without corruption:

* DOCX opens in Word or Google Docs
* XLSX opens in Excel or Google Sheets
* Markdown opens correctly
* JSON parses correctly
* CSV opens correctly

8. Regression proof

Confirm:

* Phase 1 chat still works
* Phase 2 document analysis still works
* Phase 3 dataset analysis still works
* Voice features still work
* Phase 5 image analysis still works
* Phase 6 handoff still works
* Phase 7A smart scroll/export still works
* upload drag/drop/paste still works
* Arabic voice/RTL support still works

After 7B-1 is implemented and validated, we will decide separately whether to approve:

* 7B-2 PowerPoint generation
* 7B-3 Print / Save as PDF
* 7B-4 Reporting Intelligence / reportMode

Proceed with 7B-1 implementation only.