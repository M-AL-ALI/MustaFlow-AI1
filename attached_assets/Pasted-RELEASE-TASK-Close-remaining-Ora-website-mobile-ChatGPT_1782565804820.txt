RELEASE TASK — Close remaining Ora website/mobile/ChatGPT-level gaps before any new TestFlight build

Important status:
- The gpt-4o -> gpt-5 code change is VERIFIED complete on GitHub main.
- openAiModelForOraSearch now defaults to gpt-5 for Free/Core/Wave.
- Do NOT change gpt-4o-mini-transcribe or gpt-4o-mini-tts; those are audio models.
- Still publish/redeploy the API if production has not been deployed after this model change.

Do not cut TestFlight until all items below pass.

1. Sync and deploy model update
- Pull latest GitHub main.
- Confirm HEAD includes the gpt-5 search model change.
- Run:
  - api-server typecheck
  - model-router.test.ts
  - routing-diagnostics.test.ts
- Publish/redeploy API.
- Verify production routing diagnostics show Ora search OpenAI model = gpt-5.
- Confirm no production path still uses gpt-4o for Ora chat/search.

2. Provider routing live verification
- Run the sanctioned admin Ora routing diagnostics endpoint.
- Verify OpenAI fallback works with gpt-5.
- Verify Anthropic is callable if configured.
- Verify Gemini is callable if configured.
- Verify DeepSeek decision:
  - If DeepSeek should be active, DEEPSEEK_DISABLED must not be true and DEEPSEEK_API_KEY must work.
  - If DeepSeek remains disabled, document it as intentional.
- Confirm provider fallback chain works when first provider fails.
- Confirm no Builder routing leaks into Ora.

3. Talk to Ora realtime QA — website
- Test in real browser, signed in.
- Verify Talk to Ora uses realtime WebRTC, not legacy transcribe/chat/TTS unless fallback is triggered.
- User speaks -> Ora responds instantly with audio, ChatGPT-style.
- Barge-in: user interrupts while Ora is speaking -> Ora audio stops immediately and listens.
- Mute: mutes Ora output only; mic still listens.
- End: stops mic, audio, peer connection, timers.
- Language: spoken audio and visible transcript must match the same language.
- Test Arabic, English, and Auto Detect.
- Test network drop -> visible fallback to legacy loop, no stuck UI.
- Test long session duration cap.

4. Talk to Ora realtime QA — iPhone
- Requires fresh native build with react-native-webrtc.
- Test on real iPhone, not simulator only.
- Test built-in mic, speaker, AirPods/Bluetooth.
- Verify instant speech-to-speech behavior.
- Verify barge-in hard-stop.
- Verify no overlapping Ora audio after interruption.
- Verify app background/foreground behavior.
- Verify lock screen / call banner / Control Center interruptions.
- Verify fallback notice if realtime cannot start.
- Verify language consistency: audio voice language = text transcript language.

5. Mic dictation QA
- Mic is separate from Talk to Ora.
- Mic should record voice, transcribe it, and place transcript in composer.
- Mic should NOT auto-send.
- Transcript must be editable before sending.
- Test permission deny/grant.
- Test silence / noisy recording / network failure.
- Test language transcription.

6. Plans / features / entitlement QA
- Test real Free, Core, and Wave accounts.
- Website and mobile must show same tier.
- Mobile Settings diagnostics:
  - signed in = yes
  - Clerk token = present
  - billing tier = expected tier
  - chat tier = same tier
- Free:
  - purple accent
  - Deep Thinking locked
  - free limits enforced
- Core:
  - blue accent
  - Deep Thinking enabled
  - Core limits enforced
- Wave:
  - amber accent
  - Deep Thinking enabled
  - Wave limits enforced
- If billing tier and chat tier mismatch, do not patch UI; fix auth/session/backend tier resolution.

7. iOS payment/App Store compliance
- Confirm iOS v1 has web-only purchases:
  - no Stripe checkout link in iOS
  - no Upgrade button opening Stripe
  - no Manage billing external Stripe portal link
  - no add payment method flow inside iOS
- Existing paid users from website must still unlock Core/Wave in iOS.
- Website Stripe billing remains active.
- If future iOS in-app purchase is needed, plan StoreKit/RevenueCat separately.

8. Visual parity
For each screen, compare website screenshot, mobile screenshot, and website source.
Do not move to next screen until approved.
Screens:
- Main empty chat
- Chat with messages
- Streaming/thinking state
- Composer
- Mic dictation
- Talk to Ora panel
- Drawer/sidebar
- Settings
- Memory
- Library
- ORAX
- Help
- Light mode and dark mode

9. Performance QA on iPhone
- 50+ message conversation.
- Long streaming answer.
- Source-heavy answer.
- Image-heavy answer.
- Generated-file answer.
- Scroll while streaming.
- Open message actions on old messages.
- Save memory chip after long stream.
- Follow-up suggestion chip after long stream.
- No jank, no flicker, no dropped state.

10. Export QA
- Verify mobile chat exports:
  - Markdown
  - JSON
  - CSV/action plan
  - Word .docx via backend export-file
  - Excel .xlsx via backend export-file
  - Presentation .pptx via backend export-file
  - PDF on rebuilt native app
- Verify generated files save/share correctly.
- Verify PDF requires fresh native build with expo-print.

Definition of done before TestFlight:
- API published with gpt-5 model change.
- Provider routing diagnostics pass.
- Website Talk to Ora QA passes.
- iPhone Talk to Ora QA passes.
- Mic dictation QA passes.
- Free/Core/Wave plan diagnostics pass.
- iOS payment compliance confirmed.
- Visual parity approved screen by screen.
- Performance QA passes.
- Export QA passes.
- Then cut a fresh native TestFlight build.