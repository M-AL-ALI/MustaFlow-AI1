“Good investigation. Please proceed, but the task must be focused on first-token latency only. Do not change the existing word-by-word streaming cadence on website or mobile. The goal is to reduce the time before the first visible word appears across all three surfaces: authenticated Ora website, public landing-page Ask Ora bubble, and Ora Mobile. The production logs show server TTFT is already around 1.0–1.7s on the current fast path, which means the current model/provider route cannot reliably hit a ≤1s first-word target by client changes alone. Also, one surface is still silently using non-streaming /api/public-ai/chat, where first visible word equals full reply time around 2.6s. That must be identified and fixed.
Please implement this as a measured latency task, not a streaming UI task:
Add shared request/client diagnostics:Generate a request ID on the client and send it to /api/public-ai/chat/stream.
Include surface: web-authenticated, web-landing, or mobile.
Include mode, transport type, and whether fallback happened.
Client logs should record sendPressedAt, requestStartedAt, headersAt, firstTokenAt, doneAt, fallbackReason, and tapToFirstTokenMs.
Server logs should include the same request ID and surface, plus classifierMs, routingMs, modelStartMs, model/provider route, fast-lane yes/no, and fallback yes/no.

Find and fix the non-streaming caller:Production logs show /api/public-ai/chat is still being hit for a user-facing Ora response.
Identify whether it is mobile, landing Ask Ora, fallback from stream, or another path.
For normal Instant chat, all three surfaces must attempt /api/public-ai/chat/stream first.
/api/public-ai/chat should only be used for true fallback or specialist non-streaming flows.
If mobile falls back, diagnostics must show exactly why.

Fix or bypass the broken classifier:Logs show gpt-5-nano classifier takes 1.5–2.7s and returns empty/defaults repeatedly.
For obvious Instant prompts, do not wait for it and ideally do not even launch the expensive classifier in the critical path.
For non-obvious prompts, cap classifier impact tightly and log when it times out or returns empty.
If classifier remains broken, disable it for Instant simple prompts and use deterministic routing gates instead.

Reduce model TTFT:Current fast route using gemini-3-flash-preview still shows ~1.0–1.7s server TTFT, which misses the ≤1s goal before network/client rendering.
For simple Instant prompts, route to the fastest reliable model available, not the best/deepest model.
Benchmark candidate models in production-like conditions using the same /chat/stream route.
Choose the model/route that gives first token under 1s for simple Instant prompts.
Keep Deep mode, research, file/image generation, and complex analysis on higher-quality routes; do not reduce their quality.

Prewarm and cache:Confirm website authenticated Ora, landing Ask Ora bubble, and mobile all prewarm /api/public-ai/session.
Cache or prefetch lightweight profile/memory context where safe.
For Instant simple prompts, if context is not ready inside a very small budget, proceed without blocking first token.
Do not let memory/history/context turn a simple Instant prompt into 3–5 seconds.

Landing Ask Ora must be included:The landing page Ask Ora bubble is a first impression and must be optimized the same as /ora.
Do not only fix signed-in Ora.
Public/anonymous session handling must not add noticeable first-token delay.

Mobile must be included:TestFlight must be verified with diagnostics.
Mobile should show whether it used XHR SSE streaming, whether fallback happened, and tap-to-first-token time.
If mobile still falls back or buffers, fix that path, but do not change the word-by-word pacing itself.

Acceptance criteria:
Simple Instant prompts like ‘hi’, ‘what is 2+2’, and ‘summarize this in one sentence’ should show first visible token in ≤1s on healthy network where technically possible.
If any request exceeds 1s, logs must show the exact slow bucket.
No regression to word-by-word streaming on website or mobile.
No regression to Deep mode quality, research, files, images, video, or Talk to Ora.
Add tests/source guards proving all three surfaces use /chat/stream first and send a surface field/request ID.
After deploy/TestFlight, run manual QA on authenticated website Ora, landing Ask Ora bubble, and Ora Mobile, with production logs confirming first-token timing.”