Separate task request — Ora chat thread cleanup, Talk with Ora speed, and safe coding capability.

Please treat this as a separate task from Phase 7B file generation.

Issue 1 — Ora replies overlap / chat thread not organized

Current problem:
Ora replies are coming in overlapping, crowded, or not organized cleanly in the chat thread.

Please investigate and fix:

* message spacing
* assistant/user bubble layout
* long response wrapping
* code block wrapping
* markdown rendering
* dataset/report card spacing
* voice transcript spacing
* image/document analysis response spacing
* mobile bubble layout
* scroll container height
* auto-scroll behavior
* “Latest” button placement
* overlapping action buttons / export menu / handoff card

Expected result:

* every message is clearly separated
* user messages and Ora messages are easy to distinguish
* long Ora replies do not overlap other UI
* action buttons do not cover text
* export buttons do not crowd message content
* mobile chat thread remains clean
* previous messages remain readable
* latest message is easy to reach

Issue 2 — Talk with Ora takes too long to reply

Current problem:
Talk with Ora / Voice Conversation Mode is taking too long to respond.

Please investigate:

* Whisper transcription latency
* audio recording stop timing
* silence detection delay
* API transcription time
* Ora response model latency
* TTS start delay
* whether voice mode waits too long before sending
* whether it is doing extra unnecessary calls

Recommended improvements:

* show clear states:
    * Listening
    * Transcribing
    * Thinking
    * Speaking
* reduce silence delay if safe
* start processing immediately after user stops
* avoid duplicate transcription/model calls
* avoid blocking UI while waiting
* add timeout and safe retry message
* keep the transcript visible immediately
* allow user to cancel/stop while waiting
* show progress so user knows Ora is working

Important:
Do not sacrifice transcript accuracy or safety just for speed.

Issue 3 — Can Ora write and generate code for visitors?

Yes, Ora should be able to help visitors with code, but with clear boundaries.

Approved coding capability for public Ora:

Ora can:

* write code snippets
* explain code
* debug code pasted by visitor
* generate frontend examples
* generate backend examples
* generate SQL examples
* generate AppSheet formulas
* generate Excel formulas
* generate Python/JavaScript/TypeScript examples
* explain errors
* suggest architecture
* write pseudocode
* create starter templates
* create safe example files for download later if file-generation supports it

Ora must NOT:

* access Builder projects
* edit real project files
* deploy code
* run production commands
* access secrets/API keys
* connect to databases
* modify user apps
* create real Builder projects unless user uses the authenticated Builder handoff
* pretend it changed code inside MustaFlow Builder

If visitor asks:
“Build this app for me”
Ora should say:
“I can help plan and generate example code here, but building the real app happens inside MustaFlow Builder.”

Then show Continue in Builder only if there is real build/app intent.

Coding response UX:

* code blocks should render cleanly
* add copy button for code blocks if possible
* code blocks should not overlap the chat thread
* long code should scroll horizontally or wrap safely
* explain where the code belongs
* include warnings for secrets/API keys
* do not ask users to paste private keys
* do not include fake production credentials

Recommended new capability name:

“Ora Code Assist”

Public visitor mode:

* educational and planning code help
* safe snippets
* debugging support
* architecture guidance

Authenticated Builder mode:

* real app/code generation and project changes

This keeps public Ora helpful while preserving the boundary between visitor chat and the Builder.

Required validation report:

1. Chat thread UI proof

* before/after screenshot if possible
* no overlap in long Ora response
* no overlap in code block
* no overlap in dataset/report card
* no overlap on mobile
* action buttons do not cover text
* export menu does not cover text

2. Voice speed proof

* measured average time from stop speaking to transcript
* measured average time from transcript to Ora reply
* measured average time from Ora reply to speech playback
* confirm no duplicate calls
* confirm loading states are clear

3. Code capability proof

* Ora can generate a simple JavaScript snippet
* Ora can explain an error
* Ora can generate SQL or AppSheet formula
* Ora refuses to access Builder/project files from public mode
* Ora guides user to Builder for real app creation
* code blocks render correctly
* copy code works if implemented

4. Privacy/safety proof

* no secrets requested
* no API keys logged
* no code stored permanently
* no Builder/project access
* no deployment access
* no database access

5. Regression proof

* Phase 1 chat still works
* Phase 2 document analysis still works
* Phase 3 dataset analysis still works
* voice features still work
* Phase 5 image analysis still works
* Phase 6 handoff still works
* Phase 7A scroll/export still works
* Phase 7B export generation still works

6. Quality checks

* typecheck
* lint
* format
* frontend workflow
* backend workflow

Do not start Web Search, Knowledge Vault, Image Generation, Advanced Charts, or unrelated roadmap work under this task.