Work on the current MustaFlow-AI repository and fix the preview and AI-builder runtime problems end to end.

Goal:
When the AI agent builds an application, the user must be able to test the real current draft app before publishing, including frontend, backend/API routes, logs, preview-safe environment variables, and database behavior where supported. Keep fast UI preview, but add a real full-app preview.

Important:
- Inspect the current code first and confirm each issue before changing it.
- Do not remove existing working functionality.
- Implement in small safe steps.
- Add tests for every security or publishing behavior change.
- Before coding, give me a short implementation plan listing routes, components, database/schema changes, and container functionality you will reuse.

Files already relevant:
- `artifacts/mustaflow/src/pages/projects/components/preview-tab.tsx`
- `artifacts/mustaflow/src/hooks/use-web-container.ts`
- `artifacts/api-server/src/routes/files.ts`
- `artifacts/api-server/src/routes/containers.ts`
- `artifacts/api-server/src/lib/serveSnapshot.ts`
- `artifacts/api-server/src/lib/consoleBridge.ts`
- AI builder/job/task routes and agent execution files
- project version, publishing, secrets and database provisioning logic

PHASE 1: Fix preview security immediately

1. Make the editor preview route private at all times.
- `/api/projects/:id/preview/*` must always require authenticated project ownership.
- Do not make this route public after a project is published.
- A published project may still have unpublished draft edits, and those drafts must never be publicly accessible.

2. Public published output must use only published content.
- Public slug URLs and custom domains must serve the frozen `publishedSnapshotId` version or its production deployment.
- Editing a draft after publishing must never change public output until the user publishes again.

3. Fix failed production-container fallback.
- If a production container fails or times out, public requests must correctly fall back to the most recent frozen published snapshot.
- Do not return an empty or failed response when a valid published snapshot exists.

4. Remove editor-only instrumentation from public apps.
- Do not inject the studio console/crash bridge into public published HTML.
- Keep console/crash capture only inside authenticated preview sessions.

PHASE 2: Create two clear preview modes

Keep the current fast preview, but label it correctly.

1. Quick Preview
- Preserve the existing static/WebContainer preview.
- Clearly label it `Quick Preview`.
- Explain through status text that it tests frontend behavior only.
- It may support UI, CSS, browser navigation and client-side interactions.

2. Full App Preview
- Add a `Full App Preview` mode in the Preview tab.
- It must start or wake an isolated development container for the current draft project.
- Sync the current draft project files into that container.
- Run the project using its actual development/runtime command.
- Display the real running preview container inside the iframe.
- Do not display a container status while rendering a different static/WebContainer runtime without making that distinction clear.

Full App Preview must support, wherever the generated project supports them:
- frontend routes
- backend/API routes
- server-side behavior
- runtime logs and server errors
- project environment variables using preview-safe secrets
- database access using isolated preview data or a clearly marked safe development database
- authentication testing configuration
- restart and stop controls
- automatic sleep/cleanup after inactivity

PHASE 3: Connect AI agent output to real testing

The AI agent creates real app code, but the preview workflow must accurately test that output.

1. Main Agent
- When the Main Agent writes draft files, Quick Preview should refresh as it does now.
- Full App Preview should sync/rebuild/reload the running draft runtime.

2. Task Agent
- Task Agent changes must remain staged until the user applies them.
- Staged changes must not silently enter the live preview or published output.
- After Apply, sync the accepted files into Quick Preview and Full App Preview.

3. Preserve agent identity in queued/background jobs.
- Verify queued tasks and background runs retain whether they belong to Main Agent or Task Agent.
- Do not allow a staged Task Agent job to default into Main Agent behavior and directly overwrite live files.

4. Database/migration behavior
- If the agent generates database changes, Full App Preview must clearly run required preview migrations or report that the preview cannot start.
- Migration failures must not leave the project in a misleading partially-working state.

PHASE 4: Preview secrets, database and isolation

1. Preview must not use production customer data or production secrets by default.
- Add preview-safe environment handling.
- Clearly separate preview environment configuration from production deployment configuration.

2. Full App Preview must remain owner-only.
- Authenticate access to the preview container or proxy it through an authenticated backend route.
- Do not expose raw draft preview containers publicly.

3. If a shareable preview feature is added later:
- Use explicit user action.
- Use signed expiring URLs.
- Use isolated preview hosting/subdomains.
- Never use the private editor preview route as public sharing.

PHASE 5: Fix confusing and incomplete preview behavior

1. Make the UI accurately show which runtime is visible:
- `Quick Preview - Static`
- `Quick Preview - WebContainer`
- `Full App Preview - Development Container`
- `Published Version`

2. Ensure owner mock/testing tools still work after the project has been published.
- Published status should not disable authenticated draft testing features for the owner.

3. Improve WebContainer behavior:
- Avoid unnecessary repeated `npm install` runs where safe.
- Prefer lockfile-based deterministic installs and caching where possible.
- Preserve fallback behavior when browser WebContainer support is unavailable.

4. Mobile preview:
- Clearly distinguish web-on-phone preview from an actual native/EAS build.
- Do not present browser preview as proof that native functionality works.

REQUIRED TESTS

Add automated tests covering:

1. An unauthenticated user cannot access `/api/projects/:id/preview/*`, even when the project is published.
2. A project owner can still preview their draft after publication.
3. Draft edits made after publication do not appear at the public slug URL or custom domain.
4. If the production container fails, the published snapshot is served successfully.
5. Public published HTML does not contain the editor console/crash bridge.
6. Quick Preview correctly identifies static versus WebContainer rendering.
7. Full App Preview loads the running draft container URL, not the static preview URL.
8. Full App Preview can execute a generated backend/API route.
9. Full App Preview reports server runtime errors/logs.
10. Task Agent staged work does not affect live preview before Apply.
11. Queued Task Agent work preserves Task Agent identity and does not overwrite live files.
12. Applying staged work updates preview correctly.
13. Preview secrets/database configuration does not default to production data.
14. Mobile UI distinguishes browser preview from real native build testing.

DELIVERABLES

After implementation, provide:
- summary of changed files
- database/schema migrations, if any
- security behavior changes
- screenshots or a short demonstration of Quick Preview and Full App Preview
- test results
- any functionality still unsupported, especially auth, database, native mobile or external integrations

Do not claim the preview is a real full-app testing environment until a generated app with a frontend page, backend API route, runtime log/error and preview-safe database/environment behavior has been tested successfully inside Full App Preview.