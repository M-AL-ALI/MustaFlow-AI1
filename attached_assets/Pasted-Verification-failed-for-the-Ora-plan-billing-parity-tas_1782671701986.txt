Verification failed for the Ora plan/billing parity task.

Latest main is 6c62480, but it only contains another mobile auth/session race fix. The Ora-only billing refactor did not land.

Still present:
- Mobile Settings PlanFeatureCards still renders:
  - monthlyCredits / credits per month
  - maxConcurrentBuilds / concurrent builds
- Website pricing still contains Builder features:
  - Builder credits / month
  - Built with MustaFlow badge
  - Connectors (GitHub & more)
  - concurrent builds
- Website billing still hardcodes Deep Wave priceUsd: 65.
- Server/mobile canonical Deep Wave price is still TIER_PRICE_USD.wave = 40.
- No ORA_TIERS_META exists.
- No oraTiers field exists on /api/billing/subscription.
- Mobile still renders subscription.tiers, not Ora-only tiers.
- Website billing/pricing still use hardcoded plan arrays.

Passing checks:
- ora-mobile account-sync-wiring.test.ts: 27/27
- ora-mobile typecheck: pass
- api-server typecheck: pass
- mustaflow typecheck: pass

But those checks only prove auth/session wiring is okay. They do not prove Ora plan parity.

Please implement the actual Ora plan/billing task:
1. Add server-side Ora-only tier metadata, e.g. ORA_TIERS_META.
2. Add oraTiers to GET /api/billing/subscription.
3. Ensure Ora tier metadata includes only Ora features:
   - Ora messages
   - Ora images
   - Talk to Ora minutes
   - reset windows
   - Instant replies
   - Deep Thinking
   - memory/history/language/voice if applicable
   - support level if applicable
4. Remove Builder features from Ora plan cards:
   - Builder credits
   - concurrent builds
   - build queue
   - Built with MustaFlow badge
   - Builder connectors
5. Fix website/server/mobile price parity:
   - Decide canonical Deep Wave price.
   - If canonical is $40, update website billing/pricing hardcoded $65 to $40 or, better, render from server oraTiers.
   - If canonical is $65, update server TIER_PRICE_USD.wave and Stripe/server metadata consistently.
6. Mobile Settings should render oraTiers, not subscription.tiers.
7. Website Ora billing/pricing/settings should render the same Ora-only server metadata, not duplicated hardcoded arrays.
8. Keep mobile payment behavior web-managed:
   - no Stripe checkout UI
   - no billing portal UI
   - no payment setup UI
   - copy can say plans are managed on the MustaFlow website.
9. Add tests proving:
   - mobile Ora plan cards do not contain Builder words/features
   - website Ora plan cards do not contain Builder words/features
   - Deep Wave price matches across server, website, mobile
   - mobile has no checkout/portal/payment setup wiring
   - both website and mobile consume the same server Ora tier metadata

Do not mark the Ora plan/billing parity task complete until these are implemented and verified.