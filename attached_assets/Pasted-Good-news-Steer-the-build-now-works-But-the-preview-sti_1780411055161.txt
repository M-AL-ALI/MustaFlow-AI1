Good news: Steer the build now works.

But the preview still does not load. The UI shows:

- 4 validation issues found
- TypeScript typecheck failed
- Agent loop terminated: step-cap
- Quality checks flagged server startup
- Preview refresh requested but app server is not reachable
- Preview unavailable: couldn’t reach the dev server inside the container

This means the core issue is now server/runtime startup, not Developer Mode and not the preview iframe alone.

Please do not treat this as a successful build yet.

Required next fix:
Diagnose why the dev server inside the container is not reachable for project 86 “Towco Fullstack.”

Please inspect:

1. Container logs
2. npm run dev / dev:server command output
3. server-start check output
4. TypeScript error output
5. package.json scripts
6. missing dependencies
7. whether the generated app is React/Vite, Expo, or Node API
8. whether Zero generated the wrong project structure
9. whether the app is trying to run Expo when this should be web/fullstack
10. whether the server is listening on the expected port
11. whether healthz route exists and responds
12. whether package install completed successfully

Important:
The final state should not be “preview unavailable.” Zero must either:
- fix the server startup and reach preview_ready, or
- stop with a clear blocked status explaining the exact reason.

Also fix the step-cap issue:
If Zero hits step-cap before the app is runnable, it should not just end with warnings. It should summarize:
- what failed
- what it tried
- what is still blocking preview
- exact next repair needed

Expected behavior:
If preview is unavailable because server-start failed, the “Ask AI to fix this” action should send a targeted repair prompt such as:

“Fix the server startup failure first. Read the container logs, inspect package.json scripts, verify the correct framework, install missing dependencies, make sure the dev server listens on the expected port, and do not add new features until healthz and preview are reachable.”

Please run one focused repair task for project 86:
- no new features
- fix server startup first
- fix missing dependency/typecheck second
- verify preview_ready only after HTTP 200
- report final status honestly