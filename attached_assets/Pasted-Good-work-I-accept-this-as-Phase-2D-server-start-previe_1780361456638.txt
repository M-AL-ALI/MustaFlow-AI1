Good work. I accept this as Phase 2D server-start / preview-chain blocker fixed.

Please checkpoint this state.

Important distinction:
This proves the crashed-server repair path and preview-chain trigger are working better, but it does not yet prove the generated app is fully clean, user-testable, and production-ready.

Accepted fixes:

* DATABASE_URL import-time crash identified.
* Lazy DATABASE_URL guidance added.
* Crashed-server bypass added so the agent can run when /healthz fails but container exec works.
* server-start healthz check added.
* node-syntax / entrypoint issue fixed.
* shell syntax bug fixed.
* preview chain triggered.
* final verification task completed in about 4 minutes.

Remaining concerns:

1. TypeScript typecheck is non-blocking now.
    That is acceptable for keeping preview repair moving, but it must not let the app look production-ready if typecheck fails.

Please confirm:

* failed typecheck still appears in final report
* failed typecheck marks validation as warning/failed/completed_with_errors where appropriate
* publish is blocked or warns strongly when typecheck/build validation is not clean

2. Iframe/app interaction was not fully tested.
    The iframe was auth-gated and external healthz/proxy was environment-limited, so this is not a full user-facing preview proof yet.
3. Preview chain triggered is not the same as preview-ready.
    Continue keeping these separate:

* preview_refresh_requested
* preview_unreachable_503
* preview_ready
* app_loaded
* app_tested

Next recommended phase:
Phase 2E — Clean Preview & User Test Proof

Goal:
Prove the Builder can generate or repair an app until it actually loads in preview and can be tested by a user.

Acceptance criteria:

1. App reaches preview_ready.
2. Preview iframe loads real app content.
3. At least one visible text/content check passes.
4. Navigation works.
5. One form/button interaction works.
6. TypeScript/build status is shown honestly.
7. If TypeScript remains non-blocking, final report clearly says “Preview available with validation warnings.”
8. Publish is blocked unless validation policy allows it.
9. No fake clean success.
10. Autostop restored and queue clean at end.

Do not reopen Fly/autostop, Anthropic streaming, or write_file sync unless a regression proves they broke.

Before starting Phase 2E, please give the latest commit hash and validator status for Phase 2D.