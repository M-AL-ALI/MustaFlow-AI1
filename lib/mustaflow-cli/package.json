{
  "name": "@workspace/mustaflow-cli",
  "version": "0.1.0",
  "description": "MustaFlow CLI — manage domains via the public API",
  "bin": {
    "mustaflow": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "compile": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "~5.9.3"
  }
}
