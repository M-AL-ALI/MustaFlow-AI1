Thank you. The combined upload UX + Arabic voice fix is now accepted as complete.

Accepted final checkpoint:

* 84ea348e — Remove sensitive session ID from transcription logs

Accepted related commits:

* 79426491 — drag-and-drop + paste upload, improved attachment button UX
* 509ccb12 / 282ba479 — Arabic language support and file upload functionality
* 84ea348e — sessionId removed from transcription logs

Accepted confirmations:

* working tree is clean except non-code attached_assets note
* upload button remains visible
* drag-and-drop upload is implemented
* paste-to-upload is implemented
* normal text paste remains unaffected
* uploads still use POST /api/public-ai/upload
* no new upload backend route was added
* existing file/image/dataset validation and limits remain in place
* Arabic browser SpeechRecognition uses ar-SA
* Whisper transcription sends lang=ar
* backend transcribe route validates lang
* speechToText(..., "ar") passes Arabic language hint to gpt-4o-mini-transcribe
* Arabic and RTL display uses dir="auto" on composer, message bubbles, and voice live areas
* Arabic transcript lands in composer for review/edit
* transcript text is not logged
* audio bytes are not stored
* raw sessionId is no longer logged
* success logs only include component, bytes, format, and language
* error logs do not include raw sessionId, transcript text, audio bytes, or user message text
* typecheck passed
* lint passed
* format passed
* frontend workflow is running
* backend workflow is running
* rollback plan is clear

Final staging note:

Arabic voice transcription still needs manual verification in staging/production with a real microphone and Arabic speech before production launch.

Manual Arabic staging checklist:

* select Arabic language in Ora
* test normal mic/dictation
* confirm transcript appears in Arabic script
* confirm transcript lands in composer first
* confirm user can edit transcript before sending
* send Arabic message
* confirm Ora replies in Arabic
* test Talk with Ora / Voice Conversation Mode
* confirm Arabic transcript appears in the thread
* confirm RTL layout looks correct
* confirm no transcript/audio/sessionId appears in logs

Do not start Phase 7, Phase 8, Phase 9, thread scrolling, image generation, Knowledge Vault, or unrelated roadmap work unless I explicitly approve it.