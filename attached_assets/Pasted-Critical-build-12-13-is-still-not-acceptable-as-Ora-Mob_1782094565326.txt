Critical: build #12/#13 is still not acceptable as Ora Mobile parity.

There are two separate issues:

1. Mobile chat path still needs real root-cause diagnosis.
2. Mobile still does not match the Ora website product at www.mustaflow.com in layout, theme, capabilities, or behavior.

Do not call the current mobile app “website parity.” It is still partial. The requirement is not “mobile inspired by Ora.” The requirement is:

Ora Mobile must be the native mobile version of the Ora website product.

Website Ora is the source of truth.

Required diagnostics work:

1. Confirm the latest TestFlight build is installed on device.
   - Report the visible build number in Settings → Diagnostics.

2. Diagnostics must show separate results for:
   - API base URL
   - basic transport check
   - POST /api/public-ai/session
   - POST /api/public-ai/chat with temporary “hi”
   - streaming endpoint availability, if streaming is enabled

3. If any step fails, show:
   - endpoint URL
   - HTTP status if there is a response
   - error name/message/cause if fetch fails before response
   - response body/error body if available

4. Test on both WiFi and cellular.
   - If both fail, it is not a local network blip.
   - If Safari can open https://mustaflow.com but React Native fetch fails, investigate iOS/TLS/ATS/network config.
   - If session works but chat fails, investigate Ora backend chat routing/model/provider logic.
   - If stream fails but non-stream chat works, temporarily fall back to /api/public-ai/chat until native streaming is fixed.

5. Do not use /api/healthz as proof Ora works.
   - healthz only proves the server is reachable.
   - the real acceptance test is: mobile sends “hi” to Ora and receives an Ora reply using the same website routing/plans/model logic.

Required website parity work:

Create a strict parity audit:

Website Ora item | Current mobile behavior | Difference | Required fix | Priority

Audit must include:
- layout and navigation
- light mode and dark mode
- theme tokens/colors/backgrounds/surfaces
- logo/branding/typography
- chat composer and message layout
- model/mode routing behavior
- plans/usage/billing state
- projects
- conversations/history
- memory
- library/assets
- file/image upload
- image generation/editing
- generated files
- sources/media cards
- voice/read-aloud
- temporary chat
- message actions
- ORAX
- settings
- all entitlements/feature flags

Important theme requirement:
Ora Mobile must support both light mode and dark mode like the Ora website. It should not be forced dark-only unless the website is also forced dark-only. Theme behavior, colors, surfaces, typography, and mode switching must mirror the website Ora model.

Do not continue random small patches and call it done. First compare the website and mobile screen-by-screen, then rebuild mobile to match the website Ora product exactly, while keeping the UX native/mobile-friendly.

Also confirm:
- Ora mobile uses the same backend Ora routing/plans/model logic as the website.
- Ora mobile does not use or mention AI Builder.
- No Builder/handoff endpoints exist in the mobile app.