Please implement a premium “Ora Voice Mode” experience inspired by modern AI voice buttons, but do not copy ChatGPT’s exact UI.

Goal:
Ora should have a beautiful, professional, obvious voice button that makes users feel they can talk naturally with Ora. It should look premium, intelligent, and alive — not just a plain microphone icon.

Feature name:
“Ora Voice Mode”

Button label:
“Talk to Ora”

Primary purpose:
When the user taps the button, Ora starts listening through the existing Voice-A browser-native voice system.

Design direction:
Create an original MustaFlow/Ora design using:

* Dynamic Atom identity
* soft waveform bars
* circular voice orb
* subtle glow
* smooth pulse animation
* premium black/purple/blue gradient option
* clean modern spacing
* mobile-friendly tap area

Do not copy ChatGPT’s exact icon. Use it only as inspiration for the general idea of a voice conversation button.

1. Add premium Ora Voice Mode button

Create a reusable component if needed:

artifacts/mustaflow/src/components/ora/ora-voice-mode-button.tsx

It should work in:

* Ora Panel
* Ora Bubble drawer
* mobile Ora bottom sheet/drawer

The button should replace or visually upgrade the current basic mic button.

Recommended visual concept:

Idle:

* circular button
* Dynamic Atom or waveform icon inside
* subtle shadow
* tooltip: “Talk to Ora”
* label on larger screens: “Talk to Ora”

Listening:

* animated pulse ring
* waveform bars moving
* text: “Listening…”
* button changes to stop/cancel state
* red or warm accent only for active recording, not too aggressive

Transcript ready:

* calm success glow
* text: “Transcript ready”
* transcript appears in the composer for review/edit

Processing:

* small spinner or thinking glow
* text: “Preparing response…”

Speaking:

* animated sound-wave glow
* text: “Ora is speaking”
* tapping stops playback

Unsupported:

* disabled state
* tooltip: “Voice is not supported in this browser. You can still type.”

Permission denied:

* warning state
* message: “Microphone access was denied. Enable it in your browser settings or type your message.”

2. Keep review-before-send behavior

Do not auto-send by default.

Required behavior:

* user taps Talk to Ora
* Ora listens
* live/interim transcript appears
* final transcript lands in the text box
* user can edit
* user presses Send
* transcript becomes a normal user message
* Ora replies as normal assistant message
* TTS remains optional and off by default

3. Add a better live voice area

When listening, show a small elegant voice panel above the input area:

Example:

Listening…
[animated waveform]
“Speak naturally. Your words will appear here before sending.”

If interim transcript is available, show:

“You said: [interim transcript]”

When final transcript is ready:

“Review your transcript, then press Send.”

This makes it clear that the voice feature is safe and user-controlled.

4. Button placement

Input bar should feel clean:

Recommended order:

* attachment button
* Ora Voice Mode button
* text input
* send button

On mobile:

* keep the voice button visible and easy to tap
* minimum tap target 44px
* do not cover the keyboard
* avoid layout jumping

5. Add subtle voice availability indicator

In the Ora header or input hint, show a small label:

“Voice or type in any language”

If unsupported:

“Voice unavailable on this browser — typing still works.”

6. Animation style

Use lightweight CSS animations only.

Recommended animations:

* pulse ring when listening
* waveform bar animation
* soft glow while speaking
* smooth hover scale
* no heavy animation libraries
* respect prefers-reduced-motion

If user has reduced motion enabled:

* disable pulsing/waveform movement
* use static state labels instead

7. Multilingual support

Keep the current language mapping:

* Auto Detect → browser default
* English → en-US
* Arabic → ar-SA
* Spanish → es-ES
* French → fr-FR

Do not overpromise perfect voice recognition.

Use friendly wording:

“Voice language support depends on your browser and device. You can still type in any supported language.”

8. Accessibility

The button must include:

* aria-label
* tooltip
* keyboard focus state
* visible focus ring
* support Enter/Space activation
* screen-reader friendly state text

Suggested aria labels:

* “Start talking to Ora”
* “Stop listening”
* “Ora is listening”
* “Voice input is not supported”
* “Microphone permission denied”

9. Privacy

Do not change the current privacy model.

Voice-A rules remain:

* no backend voice route
* no audio upload
* no audio storage
* no transcript logging
* no Builder/project memory access
* final transcript is only stored as temporary text after the user sends it
* TTS is optional and user-controlled

10. Do not add backend routes

Do not add:

* /api/public-ai/transcribe
* /api/public-ai/tts

This is still Voice-A visual/UX improvement only.

Voice-B backend fallback remains deferred.

11. Add polished microcopy

Use premium, friendly wording:

Idle:
“Talk to Ora”

Listening:
“Ora is listening…”

Interim:
“Keep speaking…”

Transcript ready:
“Review your transcript, then send.”

Speaking:
“Ora is speaking…”

Unsupported:
“Voice is not supported in this browser. You can still type.”

Permission denied:
“Microphone access was denied. Enable microphone access or type your message.”

12. Integration with existing Voice-A

Reuse existing:

* use-ora-voice.ts
* existing voice state machine
* existing transcript review flow
* existing TTS toggle
* existing sessionStorage behavior
* existing Ora Panel and Ora Bubble structure

Do not rewrite the voice system from scratch unless necessary.

13. Tests required

Add or update tests for:

* Ora Voice Mode button renders in Ora Panel
* Ora Voice Mode button renders in Ora Bubble
* mobile drawer shows button
* idle state renders correctly
* listening state renders waveform/pulse
* transcript-ready state lands text in composer
* unsupported browser state renders safe fallback
* permission denied state renders safe fallback
* speaking state shows stop behavior
* reduced-motion mode does not animate aggressively
* aria-labels exist
* keyboard activation works
* no backend voice routes added
* no transcript text logged
* Phase 1 chat still works
* Phase 2 document analysis still works
* Phase 3 dataset analysis still works
* Phase 5 image analysis still works
* Phase 6 handoff still works

14. Validation report required

After implementation, provide:

* affected files
* screenshots of Ora Panel voice button
* screenshots of Ora Bubble voice button
* mobile screenshot
* idle/listening/transcript-ready/speaking/unsupported states proof
* accessibility proof
* reduced-motion proof
* no backend route proof
* no privacy regression proof
* tests run
* typecheck/lint/format status
* rollback plan

15. Final expectation

Surprise me with a premium voice experience.

I want the button to feel like Ora is alive and ready to talk, but still professional and clean.

It should look original to MustaFlow/Ora, not copied from ChatGPT.

Make it beautiful, smooth, simple, trustworthy, and production-ready.