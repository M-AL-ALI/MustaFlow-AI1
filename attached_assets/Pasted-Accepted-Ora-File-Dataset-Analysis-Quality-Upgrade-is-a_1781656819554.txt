Accepted. Ora File + Dataset Analysis Quality Upgrade is approved.

Completed:

* deeper document analysis for PDF/DOCX/TXT
* structured document framing
* targeted-question vs full-analysis mode
* mode-aware token budget
* domain expert framing for uploaded files
* dataset outlier detection using IQR/Tukey method
* duplicate-row detection
* data quality block surfaced to the model
* source-honesty rules
* injection-safe domain detection
* dataset JSON-only behavior preserved
* architect-flagged domain detection bug fixed
* regression tests added
* full API suite passing: 1252 passed / 2 skipped
* typecheck, Prettier, codegen drift clean
* Ora isolation still passing

Do not call the full Ora quality roadmap finished yet. Move to the next phase:

Ora Professional Output + File Creation Upgrade

Goal:
When the user asks Ora to create, modify, or export a professional file, Ora should generate polished deliverables that are ready to use.

Next requirements:

1. Professional document creation
    Ora should create high-quality:

* executive summaries
* audit reports
* KPI reports
* SOPs
* checklists
* business plans
* meeting summaries
* action plans
* root-cause reports
* training documents
* accounting summaries
* process-improvement reports

2. Strong formatting
    Generated documents should include:

* clear title
* executive summary
* sections/headings
* tables when useful
* action items
* owner/date/status columns when useful
* recommendations
* next steps
* clean professional wording

3. Export support
    If supported by the current system, improve generated:

* DOCX
* PDF
* CSV
* XLSX

If PDF/XLSX export is not currently supported, do not fake it. Report what is supported and add missing formats as follow-up tasks.

4. Modify existing file behavior
    When user uploads a file and asks to modify it:

* preserve the original meaning
* make requested edits clearly
* do not hallucinate content
* mention assumptions
* provide a clean revised version
* optionally generate a new downloadable file if supported

5. Dataset-to-report output
    When user uploads CSV/XLSX and asks for a report:
    Ora should generate a professional report based on:

* KPIs
* missing values
* outliers
* duplicate rows
* trends
* top categories
* risks
* recommendations
* action plan

6. Tests
    Add tests for:

* DOCX/report generation quality
* CSV-to-KPI report quality
* professional formatting
* modification of uploaded document
* no fake unsupported export claims
* no hallucinated file facts
* Ora isolation still passes

Acceptance:
This phase is complete only when Ora can turn analysis into polished professional deliverables that users can copy, download, or use directly.

Keep Ora fully isolated:
No AI Builder.
No Builder.
No handoff.
No shared flow.