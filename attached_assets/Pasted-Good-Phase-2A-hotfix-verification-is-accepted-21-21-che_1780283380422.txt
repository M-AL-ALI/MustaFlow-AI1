Good. Phase 2A hotfix verification is accepted.

21/21 checks passed, validators are green, and the permanent regression test is in place:

pnpm –filter @workspace/scripts run verify-write-file

Now start Phase 2B: Anthropic Large-Context Streaming / QA Auto-Fix Stability.

Problem:
During Phase 1/2A verification, the QA auto-fix path hit the Anthropic SDK non-streaming 10-minute guard for large prompts. This caused the QA auto-fix to fail or warn instead of reliably repairing large-context issues.

Goal:
Make large QA auto-fix / repair calls reliable.

Important:
Do not reopen the Fly/autostop issue.
Do not reopen write_file / preview-sync issues unless a regression proves they broke.
Focus only on the Anthropic large-context QA auto-fix problem.

Key files:

* artifacts/api-server/src/lib/ai-providers.ts
* artifacts/api-server/src/lib/builder.ts around the QA auto-fix call site near line 1718
* Any model-router/helper files involved in Anthropic calls

Required audit first:
Please inspect the Anthropic call path and answer:

1. Where exactly is the QA auto-fix call made?
2. Which model is used?
3. Is it using non-streaming or streaming?
4. What timeout/guard is being hit?
5. How large is the prompt/context usually?
6. Is the failure caught and surfaced to the user, or only logged as warning?
7. Can this failure incorrectly make the task look successful?
8. Which other call sites use Anthropic non-streaming with large prompts?

Implementation requirements:

1. Use streaming for large Anthropic QA/fix calls
    If the prompt/context is large, route the Anthropic call through a streaming path instead of non-streaming.
2. Add size-based routing
    Example logic:

* Small prompt: non-streaming okay
* Large prompt or QA auto-fix: streaming required
* Very large prompt: summarize/truncate first, then stream

3. Add timeout safety
    Do not let the QA auto-fix silently hang for 10 minutes.

Add:

* clear timeout
* retry/backup path
* final explicit warning if repair cannot run

4. Add prompt compression/summarization when needed
    Before QA auto-fix, include only what is needed:

* failing check output
* affected files
* recent diffs
* relevant source snippets
* current task goal
* previous repair attempts

Avoid sending the entire project if not needed.

5. Surface failure clearly
    If QA auto-fix cannot run because of model timeout/context size:

* show this in the task report
* show it in Builder chat
* do not hide it as a non-fatal warning only

6. Do not fake success
    If QA auto-fix fails and validation still fails:

* task should be completed_with_errors or failed depending on severity
* final summary should clearly say QA auto-fix could not complete

7. Add regression test
    Create a test that simulates or uses a large QA auto-fix context.

Verify:

* large prompt takes streaming path
* no non-streaming 10-minute guard is hit
* timeout is handled safely
* failure is reported clearly if the model call fails
* task result does not look like clean success when QA auto-fix fails

Acceptance criteria:

* Anthropic large QA auto-fix no longer uses fragile non-streaming path.
* Large context is streamed or summarized before model call.
* Timeout/failure is surfaced clearly.
* Validation/task status remains honest.
* Typecheck/lint/format/quality gate all pass.
* Provide a concise final report with files changed, tests run, and remaining risks.