ORA RELEASE GAP LIST — DO NOT BUILD UNTIL CLOSED

Current verified good:
1. Website + mobile both use the same backend Ora chat/session APIs.
2. True Talk to Ora architecture exists on both web and mobile via OpenAI Realtime/WebRTC.
3. Backend mints ephemeral realtime tokens correctly; this matches OpenAI’s recommended secure pattern.
4. Mobile points to https://www.mustaflow.com in production builds.
5. Model router supports OpenAI, Anthropic, Gemini, and DeepSeek with OpenAI fallback.
6. Plans API returns Free/Core/Wave tier metadata and both web/mobile read it.
7. Deep Thinking is gated to Core/Wave in both web and mobile.
8. Mobile now calls the backend export-file route for real docx/xlsx/pptx/pdf export.
9. api-server typecheck passed.
10. ora-mobile typecheck passed.

Blocking gaps before build:
1. Current TestFlight is stale.
   - It does not prove the latest realtime/native voice fixes.
   - Do not build yet, but when all gaps close, a fresh native build is mandatory because react-native-webrtc is native code.

2. Talk to Ora still needs real ChatGPT-like QA.
   - Code now uses realtime WebRTC, but it still needs real browser + real iPhone proof.
   - Must test: instant speech-to-speech, barge-in, interruption, mute, AirPods/Bluetooth, background, lock screen, call interruption, network drop, reconnect/fallback, long session, language consistency.
   - Specific bug to verify: spoken audio language must match visible transcript language.

3. Payment / App Store risk is not closed.
   - Mobile Settings opens hosted Stripe billing flows.
   - Apple’s guideline 3.1.1 says digital features unlocked in-app generally require IAP, with region/external-link exceptions.
   - Replit must decide: implement Apple IAP/StoreKit for iOS subscriptions, or formally handle external-purchase compliance/entitlement/US-only rules.
   - Until this is decided, payment processing is not “App Store ready.”

4. Provider routing is implemented, but not live-proven.
   - Code supports OpenAI / Anthropic / Gemini / DeepSeek fallback.
   - Need live routing diagnostics proving each configured provider works:
     - Anthropic env present and callable
     - Gemini env present and callable
     - DeepSeek API key present and callable
     - OpenAI fallback works
     - circuit-breaker fallback works
   - Also review OpenAI default model IDs before release; code currently defaults many Ora paths to gpt-5.4 / gpt-5-mini.

5. Plans/features need live end-to-end proof.
   - Source says Free/Core/Wave features are wired.
   - Must test real accounts:
     - Free: purple, no Deep Thinking, free limits
     - Core: blue, Deep Thinking enabled, Core quotas
     - Wave: amber, Deep Thinking enabled, Wave quotas
   - Must verify billing tier equals chat tier in both website and mobile diagnostics.
   - Must verify upgrade, renewal, cancel, grace period, downgrade, portal, default payment method, and webhook handling.

6. Mobile formatting gate currently fails on three files.
   - Prettier check flagged:
     - artifacts/ora-mobile/app/(home)/index.tsx
     - artifacts/ora-mobile/app/(home)/settings.tsx
     - artifacts/ora-mobile/lib/api.ts
   - Run Prettier and commit before build.

7. Focused test rerun was blocked by Codex usage limit.
   - Typechecks passed, but I could not rerun the focused Vitest suites in this pass.
   - Replit must rerun before build:
     - realtime-session.test.ts
     - voice-session.test.ts
     - model-router.test.ts
     - ora-isolation.test.ts
     - billing-subscription-checkout.test.ts
     - billing-webhooks.test.ts

8. Visual parity is still not fully signed off.
   - Screen-by-screen parity is incomplete.
   - Still needs screenshot approval for:
     - composer/mic/Talk panel
     - response rendering after latest changes
     - drawer/sidebar
     - Settings
     - Memory
     - Library
     - ORAX
     - Help

9. Performance is not proven on iPhone.
   - Code has memoization and streaming improvements.
   - Need device proof:
     - 50+ message thread
     - long streaming answer
     - source-heavy answer
     - image-heavy answer
     - generated-file answer
     - scrolling while streaming
     - light/dark switching after long thread

10. Export docs are stale / need correction.
   - Old QA doc still says mobile Word/Excel/Slides are stand-ins.
   - Current code uses backend real exportFile for docx/xlsx/pptx/pdf.
   - Update docs and then test each export on device.

Definition of done before TestFlight:
1. Fix formatting.
2. Rerun full focused test gates.
3. Verify production env/provider/Stripe configuration.
4. Complete Talk to Ora real browser + real iPhone QA.
5. Complete plan/billing live QA.
6. Complete screen-by-screen visual parity approval.
7. Complete long-thread performance QA.
8. Decide and implement App Store-compliant payment strategy.
9. Only then cut a fresh native TestFlight build.