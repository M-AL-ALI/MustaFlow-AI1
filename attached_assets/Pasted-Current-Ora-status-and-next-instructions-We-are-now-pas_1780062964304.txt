Current Ora status and next instructions:

We are now past the approved Ora roadmap through Phase 6, with Phases 7–9 defined but not yet approved for implementation.

Accepted/completed main phases so far:

* Phase 1 — Public Ora chat
* Phase 2 — PDF/DOCX/TXT document upload and analysis
* Phase 3 — CSV/XLSX dataset, KPI, strategy, root-cause, 5 Whys, Fishbone, Pareto
* Phase 4 / Voice-A — browser-native voice dictation, review-before-send, optional TTS
* Phase 5 — PNG/JPG/WEBP image upload and image analysis
* Phase 6 — safe Ora → Builder handoff with short-lived opaque token

Planned but not yet approved for implementation:

* Phase 7 — Web Search
* Phase 8 — Knowledge Vault
* Phase 9 — Image Generation

Do not start Phase 7, Phase 8, Phase 9, Advanced Charts, or Voice-B backend fallback until I explicitly approve.

Right now there are separate background UI tasks, and they must stay isolated from the main roadmap.

Priority 1 — Fix the voice mistake

The premium voice orb was implemented as a replacement for the normal mic/dictation button. That is wrong.

We need TWO separate voice features:

1. Normal Mic / Dictation Button
    This must stay inside the composer/input bar.

Expected behavior:

* user taps mic
* Ora listens
* live/interim transcript appears
* final transcript lands in the text box/composer
* user can review/edit transcript
* user manually presses Send
* transcript becomes a normal user message
* Ora replies as normal assistant message
* no automatic spoken reply required
* TTS remains off unless user manually enables read-aloud

Recommended composer order:

* attachment button
* normal microphone/dictation button
* text input
* send button

2. Separate Ora Voice Conversation Mode
    The premium circular orb should become a separate “Talk with Ora” / “Ora Voice Conversation Mode” button, not the replacement for the normal mic.

Expected behavior:

* user taps “Talk with Ora”
* Ora enters Voice Conversation Mode
* user speaks naturally
* transcript appears in the message thread
* Ora can auto-send after silence or when user stops talking
* Ora replies by sound automatically
* Ora’s spoken reply also appears as assistant text in the thread
* user can interrupt Ora while speaking
* if user starts talking, Ora stops speaking and starts listening
* user can exit Voice Conversation Mode anytime
* transcript/message thread remains the source of truth

Important:
Do not add backend voice routes unless explicitly approved. Use browser-native SpeechRecognition and speechSynthesis for now.

Priority 2 — Fix Builder CTA over-triggering

The “Ready to build this? Continue in Builder” card is showing after simple greetings like:

User: “Hello”
Ora: “Hello — how can I help?”

That is wrong.

Please update CTA logic.

Do NOT show Builder CTA for:

* hello / greetings
* casual conversation
* personal advice
* voice testing
* “can you hear me”
* “what can you do”
* simple Q&A
* machine/equipment troubleshooting unless the user asks to build an app around it
* general coding explanation unless user asks to build in MustaFlow
* document/image/dataset analysis unless the user asks to build from it

Only show Builder CTA when there is real build/app intent, such as:

* user asks to build an app
* user asks if MustaFlow can build something
* user discusses app planning
* user asks to continue into Builder
* user uploads design/image/dataset/document and asks to build from it
* Ora gives an app plan or build recommendation

Also:

* hide the large CTA while Voice Conversation Mode is active
* do not interrupt the visitor while listening/speaking
* keep dismiss behavior
* show the large card only on the latest assistant message
* avoid repeated/pushy conversion UI

Priority 3 — Continue Task #1151 separately

Task #1151 is the background UI polish task for:

* message actions
* copy
* edit
* download/export
* read aloud action
* continue in Builder action

Before approving #1151, verify:

* it modifies the real files:
    * artifacts/mustaflow/src/components/ora-panel.tsx
    * artifacts/mustaflow/src/components/ora-bubble.tsx
* it does NOT create duplicate PascalCase files:
    * OraPanel.tsx
    * OraBubble.tsx
    * OraHandoffCard.tsx
    * DynamicAtom.tsx
    * OraImageChip.tsx
* it does NOT create fake DatasetResultCard.tsx unless intentionally extracted and correctly wired
* it remains frontend-only
* it does not touch:
    * backend routes
    * database
    * OpenAPI
    * Builder pipelines
    * billing/credits
    * Knowledge Vault
    * permanent message storage

Task #1151 does NOT include scrolling/thread behavior.

Priority 4 — Keep thread scrolling as a separate future background task

Thread scrolling is not part of #1151.

Create a later separate task for:

* scrollable Ora message thread
* previous-message visibility
* smart auto-scroll
* “Scroll to latest” / “New message” button
* session restore display
* mobile bottom-sheet scrolling
* message actions working on older messages

Do not mix this into #1151 unless I explicitly approve.

Required validation after the voice/CTA correction

Please provide a validation report proving:

* normal mic/dictation button is restored in the composer
* separate premium Voice Conversation Mode button exists
* dictation transcript lands in composer and does not auto-send
* Voice Conversation Mode can auto-play Ora’s reply while active
* transcript and Ora reply appear in the message thread
* user can exit Voice Conversation Mode
* user can interrupt Ora while speaking
* Builder CTA does not show after “Hello”
* Builder CTA does not show during active voice listening/speaking
* Builder CTA shows only for real build/app intent
* no backend voice routes were added
* no audio is stored or logged
* no transcript text is logged
* Phase 1 chat still works
* Phase 2 document analysis still works
* Phase 3 dataset analysis still works
* Phase 5 image analysis still works
* Phase 6 handoff still works
* Task #1151 remains separate
* typecheck/lint/format pass
* rollback plan is clear

Final instruction:

Do not start a new main roadmap phase yet.

First fix the voice UI mistake and the Builder CTA over-triggering. Keep #1151 separate. Keep thread scrolling as a separate later background task.