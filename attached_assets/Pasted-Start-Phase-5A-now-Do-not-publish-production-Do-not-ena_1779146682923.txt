Start Phase 5A now.

Do not publish production.
Do not enable mobile generation.
Do not start new visual/design work unless needed for these tasks.

The goal of Phase 5A is to move MustaFlow AI closer to private beta readiness by hardening publishing, admin security, billing credits, and SSL/domain readiness.

Work in this exact order:

1. Refactor publish/unpublish routes into publish.ts
2. Add Admin RBAC
3. Add Stripe/build credit top-up flow
4. Add Cloudflare for SaaS SSL automation foundation
5. Add production launch checklist
6. Run full validation report

==================================================
TASK 1 — REFACTOR PUBLISH / UNPUBLISH ROUTES
==================================================

Current issue:
The publish/unpublish logic is confusingly located inside duplicate.ts.

Move it into a dedicated route file:

artifacts/api-server/src/routes/publish.ts

Required endpoints:

POST /api/projects/:id/publish
POST /api/projects/:id/unpublish

Keep existing behavior:
- Validate auth
- Validate project ownership
- Generate publicSlug on first publish
- Preserve publicSlug on republish
- Freeze current project files into published snapshot
- Set published_snapshot_id
- Set status = published
- Write deployment log
- Write Knowledge Vault entry
- Return public URL
- Unpublish clears published_snapshot_id
- Unpublish disables public access
- Unpublish writes deployment log
- Unpublish writes Knowledge Vault entry

Important:
Do not break current public slug publishing, domain routing, or snapshot serving.

Acceptance:
- Publish works after refactor
- Unpublish works after refactor
- Public slug still works
- Public URL works signed out
- Draft changes do not affect published snapshot until republish
- Domain/subdomain routing still works
- Typecheck passes

==================================================
TASK 2 — ADMIN RBAC
==================================================

Current issue:
Admin page is only auth-gated. Any signed-in user can access /admin.

Fix this.

Add role-based admin access.

Roles:
- user
- admin
- owner

Implementation:
- Add user role source.
- If there is no users table, create user_profiles or app_users table.
- Store clerkUserId and role.
- Create requireAdmin middleware.
- Protect all /api/admin routes.
- Protect frontend /admin route.
- Normal users must not see admin dashboard.
- Normal users must receive JSON 403 from admin API.
- Admin users can access /admin.

Recommended:
Use ADMIN_USER_IDS env var as the first admin bootstrap method if no admin UI exists yet.

Example:
ADMIN_USER_IDS=user_abc,user_xyz

Acceptance:
- Admin user can access /admin
- Normal user cannot access /admin
- Signed-out user redirects to sign-in
- Non-admin API request returns JSON 403
- Admin API works for admin
- Admin role logic is documented

==================================================
TASK 3 — STRIPE / BUILD CREDIT TOP-UP
==================================================

Current state:
Build credits are enforced, but users need a way to buy credits.

Add billing credit top-up flow.

If Stripe keys exist:
- Use Stripe Checkout session.
- Add webhook placeholder or real webhook if safe.
- On successful payment, add credits.
- Create credit transaction record.

If Stripe keys do not exist:
- Show setup-required state clearly.
- Do not crash.
- Endpoint should return clear JSON message:
  "Stripe is not configured. Add STRIPE_SECRET_KEY to enable checkout."

Credit packages:
- Starter Pack
- Builder Pack
- Power Pack

Example:
Starter Pack: 500 credits
Builder Pack: 2,500 credits
Power Pack: 10,000 credits

Required endpoints:
POST /api/billing/checkout
GET /api/billing/credits
GET /api/billing/transactions
POST /api/billing/webhook

Billing UI:
- Show current credit balance
- Show credit packages
- Show checkout button
- Show transaction history
- Show “Stripe setup required” if keys are missing

Credit enforcement:
- Not enough credits blocks AI build
- User sees clear message
- Successful AI task deducts credits
- Failed task policy must be documented

Acceptance:
- Billing page shows credits
- Credit package UI exists
- Checkout endpoint works or returns setup-required JSON
- Transactions are recorded
- Not enough credits blocks AI build
- Typecheck passes

==================================================
TASK 4 — CLOUDFLARE FOR SAAS SSL AUTOMATION FOUNDATION
==================================================

Current state:
Domain routing and DNS verification exist.
SSL automation is still manual/partial.

Add Cloudflare for SaaS foundation.

Required env vars:
- CF_ZONE_ID
- CF_API_TOKEN
- PLATFORM_DOMAIN
- PLATFORM_CNAME_TARGET

Add database fields if missing:
- cf_hostname_id
- ssl_status
- ssl_error
- ssl_verified_at
- domain_verified_at

Required endpoint:
POST /api/projects/:id/domain/ssl-activate

Behavior:
- Require auth
- Require project ownership
- Require custom domain exists
- Require DNS/TXT verification is active
- If CF_ZONE_ID or CF_API_TOKEN is missing, return setup-required JSON
- If configured, call Cloudflare Custom Hostname API
- Store Cloudflare custom hostname ID
- Set sslStatus = provisioning
- Save sslError if failed
- Return current SSL status

Optional endpoint:
POST /api/domain/ssl-webhook

Webhook behavior:
- Receive Cloudflare SSL status update
- Update sslStatus
- Set sslVerifiedAt when active
- Store sslError when failed

UI:
- Show SSL not connected
- Show SSL setup required
- Show SSL provisioning
- Show SSL active
- Show SSL failed
- Add “Activate SSL” button
- Disable “fully live” custom domain badge until sslStatus = active

Important:
If Cloudflare env vars are missing, do not pretend SSL works.
Show clear setup-required message.

Acceptance:
- SSL activation endpoint exists
- Missing Cloudflare env vars return clean JSON
- If env vars exist, endpoint calls Cloudflare
- SSL status updates in DB
- UI shows accurate SSL state
- Typecheck passes

==================================================
TASK 5 — PRODUCTION LAUNCH CHECKLIST
==================================================

Add a Launch Readiness panel.

Location:
Publishing tab or Settings/Admin area.

Checklist items:
- Auth active
- Admin RBAC active
- AES encryption active
- ENCRYPTION_KEY present
- Public slug publishing works
- Custom domain routing works
- SSL automation configured
- Stripe configured
- Build credits enforced
- Rate limits active
- Terms page exists
- Privacy page exists
- Help page exists
- Unknown API routes return JSON 404
- No plaintext production secrets
- Mobile generation disabled
- Typecheck/build passes

Each item should show:
- PASS
- PARTIAL
- FAIL
- Setup required

Production launch must be blocked if critical items fail:
- Auth
- Admin RBAC
- AES encryption
- ENCRYPTION_KEY
- Public publishing
- Stripe setup if billing is required
- SSL setup if custom domains are enabled
- API health
- Typecheck/build

Acceptance:
- Launch checklist visible
- Each item has status
- Failed critical items block production launch
- Setup guidance shown

==================================================
TASK 6 — FINAL PHASE 5A VALIDATION REPORT
==================================================

After completing the work, run validation and provide a full report.

Grade PASS / PARTIAL / FAIL:

1. Publish route refactor
2. Publish still works
3. Unpublish still works
4. Public slug still works
5. Public URL works signed out
6. Draft vs published snapshot isolation
7. Domain/subdomain routing still works
8. Admin RBAC
9. Non-admin blocked from /admin
10. Admin API protected
11. Stripe checkout or setup-required state
12. Build credits enforcement
13. Billing transaction history
14. Cloudflare SSL activation endpoint
15. SSL status UI
16. SSL setup-required handling
17. Launch readiness checklist
18. Auth still works
19. Ownership still works
20. AES encryption still works
21. Secret masking still works
22. Export ZIP still works
23. Duplicate project still works
24. Rename/delete still works
25. AI Builder still works
26. Preview still works
27. Version history still works
28. Rollback still works
29. Knowledge Vault still works
30. Typecheck passes
31. API health passes
32. Unknown API route returns JSON 404
33. Mobile generation still disabled

Also include:
- Files changed
- Routes added/changed
- Schema changes
- Screenshots or UI proof
- API response examples
- DB proof where needed
- Known limitations
- What still needs Phase 5B

Do not publish production.
Wait for my review.