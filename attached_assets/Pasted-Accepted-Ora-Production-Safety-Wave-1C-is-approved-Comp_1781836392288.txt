Accepted. Ora Production Safety Wave 1C is approved.

Completed:

* durable spend ledger table added
* startup migration added
* spend ledger initializes on API boot
* in-memory counters seed from DB at startup
* spend caps survive simulated restart
* global cap persists
* authenticated per-user cap persists
* anonymous per-IP cap persists
* feature unit tracking persists
* concurrent requests cannot bypass caps
* threshold warning logs added at 50%, 80%, 95%, and 100%
* DB degraded mode logs ora_spend_ledger_degraded
* DB degraded mode keeps in-memory caps functional
* spend snapshot now exposes ledgerActive
* 17 new Wave 1C tests added
* final quality gate passed: 1438 tests

This closes the production cost-control gap from Wave 1B.

Important production note:
This is now restart-safe and much stronger than memory-only. For true multi-instance production, confirm the DB upsert path is atomic enough under concurrent traffic across multiple API instances. If yes, Wave 1C is production-ready. If not, add a short follow-up to make cross-instance cap enforcement fully atomic.

Next recommended phase:
Ora Production Safety Wave 2 — Monitoring, Alerts, Privacy, and Retention.

Goal:
Now that abuse limits, spend caps, provider fallback, and durable cost tracking exist, add production visibility and cleanup controls.

Wave 2 requirements:

1. Admin/internal spend dashboard or endpoint.
2. Provider fallback dashboard/log summary.
3. Error alert hooks for:
    * provider failures
    * spend cap threshold crossings
    * quota DB fail-open
    * spend ledger degraded
    * high 429 rate
4. File retention verification:
    * uploaded temp files expire
    * analyzed files are not retained longer than intended
    * generated files have a clear retention/cleanup policy
5. Anonymous session cleanup verification.
6. Privacy audit:
    * no raw file text in logs
    * no secrets in logs
    * no provider stack traces to users
    * no cross-session leakage
7. Public launch checklist.
8. Rollback controls:
    * disable Ora
    * disable streaming
    * disable file upload
    * disable expensive features
9. Live production smoke tests.
10. Quality gate and Ora isolation must still pass.

Keep Ora fully isolated:
No AI Builder.
No Builder.
No handoff.
No shared flow.