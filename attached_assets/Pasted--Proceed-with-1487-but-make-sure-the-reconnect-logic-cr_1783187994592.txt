“Proceed with #1487, but make sure the reconnect logic creates a completely fresh realtime session. Do not reuse the old PeerConnection, data channel, audio track, or ephemeral OpenAI realtime token. On reconnect, fully stop local tracks, close the old data channel and RTCPeerConnection, clear watchdogs/timers, request a new /api/public-ai/realtime/session, then rebuild WebRTC. Only attempt one automatic reconnect, then fall back to basic mode with a clear Retry button. If using @react-native-community/netinfo, confirm the package is installed and compatible with Expo/EAS; otherwise use an existing Expo-compatible reachability API. This is a native/mobile dependency change, so it requires a new TestFlight build. The Talk to Ora UI must keep fixed dimensions during poor network states so the card does not jump, resize, or visually glitch. Only the small status indicator/text should change. For audio-stop detection, avoid false positives: normal output_audio_buffer.stopped after a completed response should not count as instability. Count only suspicious stops, such as stops during an active assistant response, repeated stops without response.done, data-channel close/error, or ICE disconnected/failed. Expose networkQuality, reconnect count, last RTC state, last data-channel state, and fallback reason in diagnostics. Website parity is good, but keep the web UI minimal and do not redesign Ora website.”
For #1486 Image Save & Full-Screen Preview, the plan is also good, but tell Replit to tighten it like this:
“Proceed with #1486, but make image save/share robust for private API images, remote URLs, and base64/data URI images. On mobile, the full-screen preview should work for generated images and uploaded image attachments. The Save button should request photo permission only after tap, download the image to a temporary local file if needed, save with MediaLibrary.saveToLibraryAsync, and show success/failure feedback. Share should also use a local temp file when the source is an authenticated API URL or data URI, because native share sheets may not handle private URLs. Copy link should copy the public URL when available, but if the image is private/API-served, either copy the API URL only if it is usable with auth or show ‘Link copied may require sign-in.’ For uploaded image messages, add a real thumbnail card in the chat thread, not just filename text, and preserve the preview after reload using imageUrl, imageRef, assetId, or download URL where available. Website Ora image gallery should add Download and Copy link actions using authFetch for private asset URLs. Do not change Image Studio unless a parity issue is found.”
I’d also add this combined verification requirement:
“Verification must include mobile and website tests. Mobile: image preview modal opens from generated images, Save/Share/Copy buttons exist, uploaded image messages render thumbnails, and Talk to Ora diagnostics/reconnect UI exists. Website: Ora image gallery has Download/Copy actions and Talk to Ora status badge exists. Typechecks for ora-mobile and mustaflow must pass. Manual QA must include: generated image save to iPhone Photos, share sheet opens, uploaded image thumbnail appears in chat, website image download works, Talk to Ora under airplane-mode/weak-network simulation shows reconnecting instead of layout glitching, and recovery works when network returns.”