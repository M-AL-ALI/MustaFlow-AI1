“Build #36 did not fix Ora mobile. Do not touch Ora website. Website Ora is good. The remaining bug is mobile-only: Ora mobile still takes 3–5 seconds before showing the answer, and the reply still appears all at once instead of word-by-word. Please investigate the real TestFlight runtime path, not just source assumptions. We need proof from logs/device that streamChatNative() is actually using /api/public-ai/chat/stream on build #36. Add temporary mobile diagnostics around streamChatNative: log/record whether ReadableStream exists, whether the kill-switch returned early, whether authHeadersRequired() succeeded or threw, the exact endpoint called, HTTP status, content-type, time to response headers, time to first token, token count, whether done arrived, whether the function returned null, ok: true, ok: false firstToken:false, or ok:false firstToken:true, and whether the caller fell back to sendChat(). Also surface this in Settings diagnostics so I can screenshot it from TestFlight. The most likely issue is still that streamChatNative() is returning null on device, probably because ReadableStream is missing, response body streaming is unsupported, auth token timing fails, content-type is not text/event-stream, or the EAS build is not actually running the patched bundle. If ReadableStream is missing or res.body.getReader() is unavailable in React Native, implement a mobile-compatible SSE fallback using a proven React Native EventSource/SSE library or a fetch streaming polyfill, instead of relying on browser ReadableStream. If the stream request is succeeding but UI still paints all at once, add a mobile-side token queue that appends one token every ~55ms using setTimeout/requestAnimationFrame, independent of how chunks arrive, so React Native renders word-by-word like the website. Keep website code untouched. Verification required: new TestFlight build, Settings diagnostics shows Streaming On + ReadableStream/body reader available or SSE fallback active, mobile request hits /api/public-ai/chat/stream with 200, first token time is around 1–2 seconds, token count increments during the reply, viaFallback is false, and the UI visibly renders word-by-word