Important correction for Task #1151 before merge:

Please verify the task agent is using the real Ora component file names in the repository.

The task description mentions PascalCase files:

* OraPanel.tsx
* OraBubble.tsx
* OraHandoffCard.tsx
* DatasetResultCard.tsx
* DynamicAtom.tsx
* OraImageChip.tsx

But the actual repo files are kebab-case:

* artifacts/mustaflow/src/components/ora-panel.tsx
* artifacts/mustaflow/src/components/ora-bubble.tsx
* artifacts/mustaflow/src/components/ora/ora-handoff-card.tsx
* artifacts/mustaflow/src/components/ora/dynamic-atom.tsx
* artifacts/mustaflow/src/components/ora/ora-image-chip.tsx

Also, there is no standalone DatasetResultCard.tsx file. Dataset result rendering currently lives inside the existing Ora panel/bubble flow, so please do not create a duplicate component unless it is intentionally extracted and wired correctly.

Before merging #1151, please confirm:

1. ora-message-actions.tsx is wired into the real existing files:
    * ora-panel.tsx
    * ora-bubble.tsx
2. No duplicate PascalCase shadow files were created, such as:
    * OraPanel.tsx
    * OraBubble.tsx
    * OraHandoffCard.tsx
    * DynamicAtom.tsx
    * OraImageChip.tsx
3. The new message action component works with the existing Ora message type from the current hook/component structure.
4. Copy/Edit/Download actions are integrated into the real Ora message thread, not into unused duplicate components.
5. Dataset/document/image/voice/handoff message types still render correctly after the action buttons are added.
6. The implementation remains frontend-only unless absolutely necessary.
7. No backend routes, DB schema, Builder pipeline, billing, credit logic, Knowledge Vault, or permanent message storage are touched.
8. Phase 1–6 regression tests still pass.
9. Typecheck, lint, and format pass.
10. Provide a merge validation report showing:

* affected files
* proof no duplicate PascalCase files were created
* proof message actions appear in Ora Panel
* proof message actions appear in Ora Bubble
* proof copy works
* proof edit loads message into composer
* proof download works client-side
* proof no sensitive refs appear in exported content
* rollback plan

Also important:

Task #1151 only covers Part 1 — Professional Message Actions and Export.

It does not cover Part 2 — Conversation Thread Scrolling / Previous Messages / Scroll-to-Latest.

Please keep Part 2 as a separate follow-up background task after #1151 merges successfully.

The main roadmap is not blocked by #1151. This is an isolated frontend UI polish task and should not affect Web Search, Charts, Knowledge Vault, Voice-B, Image Generation, or any backend roadmap work.