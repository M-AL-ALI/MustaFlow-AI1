Thank you. This explanation makes sense and this fix sounds like the correct layout pattern.

The confirmed root cause is accepted:

* h-full inside the mobile drawer did not resolve correctly because the parent used max-height, not a definite height.
* The inner scroll container grew to content height.
* Overflow did not engage.
* Assistant messages continued behind the composer.
* Moving overflow-y-auto directly onto the flex-1 min-h-0 flex item is the correct fix.

Before I accept this bug fix as complete, please provide the final corrected validation details:

1. Final commit hash

Please provide the exact commit hash for this latest fix.

2. Final affected files

Confirm the final source files changed are only:

* artifacts/mustaflow/src/components/ora-bubble.tsx
* possibly artifacts/mustaflow/src/components/ora-panel.tsx
* possibly artifacts/mustaflow/index.html

3. Final behavior proof

Confirm:

* Ora message thread is visible again
* Ora sends and receives messages
* long assistant replies no longer go behind the composer
* last line of the assistant response is fully visible above the composer
* scroll works normally
* user can reach latest message
* upload hint/message count does not cover messages
* floating Ask Ora button does not overlap the open drawer
* Latest button still works

4. Device validation

Please confirm:

* iPhone Safari viewport tested or simulated
* iPhone Chrome viewport tested or simulated
* Android Chrome viewport tested or simulated
* Android/Samsung Internet compatibility considered
* keyboard-open state tested or explained
* desktop layout still works

5. Quality checks

Confirm final clean results:

* pnpm run typecheck
* pnpm --filter @workspace/mustaflow run lint
* pnpm run format:check
* frontend workflow running
* backend workflow running

6. Scope confirmation

Confirm this was only a UI/UX bug fix.

Do not mark any roadmap phase complete because of this fix.

Do not start Phase 8B-2, semantic search, RAG, AI memory, Knowledge Vault intelligence, Web Search, Image Generation, Advanced Charts, or unrelated roadmap work until explicitly approved.

I will also test on my iPhone/Android before final acceptance.