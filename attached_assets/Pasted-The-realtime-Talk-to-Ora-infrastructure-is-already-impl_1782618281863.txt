The realtime Talk to Ora infrastructure is already implemented and verified. Do not rebuild it from scratch.

Please keep the current implementation as-is unless a live QA issue appears.

Confirmed existing behavior:
- Free: 20 minutes per 5-hour window
- Core: 60 minutes per 3-hour window
- Wave: 120 minutes per 3-hour window
- Anonymous maps to Free
- Server-side metering uses /session, /heartbeat, /end, and /diagnostics
- Actual seconds are charged via heartbeat/end, not estimated text usage
- Over-limit returns 429 with reset info
- One active realtime session per user/session is enforced
- Stale sessions expire safely
- Web and mobile both use the same focus logic, voice presets, countdown, and diagnostics
- Marine/Mustafa voice presets are wired
- Provider/model names are hidden from user-facing UI
- Focused mode keeps 12s cold-start only for the first utterance, then 4s follow-up grace
- Automatic barge-in in Focused mode requires addressed/directed speech

Please verify only:
1. Latest deployed website/API includes commit 716be166bdb9dbaae99065a46e871013a4fbaeb9 or newer.
2. Both hooks contain:
   - FOCUS_COLD_START_WINDOW_MS = 12_000
   - FOCUS_FOLLOWUP_WINDOW_MS = 4_000
   - bargeInRequiresDirection()
3. Run:
   - pnpm --filter @workspace/mustaflow test src/hooks/__tests__/ora-realtime-focus.test.ts
   - pnpm --filter @workspace/api-server test src/routes/public-ai/__tests__/realtime-session.test.ts
   - pnpm --filter @workspace/api-server test src/routes/public-ai/__tests__/realtime-metering.test.ts src/lib/public-ai/__tests__/ora-realtime-usage.test.ts
   - pnpm --filter @workspace/mustaflow typecheck
   - pnpm --filter @workspace/api-server typecheck
   - pnpm --filter @workspace/ora-mobile typecheck

Do not start a new mobile/TestFlight build yet.

Next step is live QA only:
- Website Talk to Ora after publish
- Confirm no background-speaker interruption
- Confirm no language drift
- Confirm barge-in works only when intentionally directed
- Confirm speed/glitch behavior from real call diagnostics

Only make code changes if live QA shows a specific bug with evidence from logs/diagnostics.