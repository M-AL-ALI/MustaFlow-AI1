BLOCK TESTFLIGHT. Search Retry is still not fixed.

User still sees:
“I couldn't verify live results right now... Tap Retry live search...”
Even after pressing Retry, Ora returns the same fallback again.

Root issues to fix:

1. Backend search reliability
Current Retry only forces routing to the search tool. It does not make the search provider more reliable. If OpenAI web_search times out again, the same fallback repeats.

Fix required:
- Add a real force-search execution path.
- When request.forceSearch === true:
  - Use a longer search timeout than normal, e.g. 25–30s.
  - Allow one retry even after timeout, or use a secondary search strategy.
  - Log forceSearch:true, searchAttemptCount, searchProvider, searchLatencyMs, searchFailureReason.
- If live search still fails after forceSearch, do not generate the same long general-background fallback again. Return a clear retryable search error:
  “Live search is temporarily unavailable. I could not verify current results.”
  Keep the user message and allow Retry again.
- For current/news/latest prompts, do not make general knowledge look like verified current news.

2. Add a secondary live-search fallback
OpenAI web_search timeout is still a production failure point. Retry must not simply call the same failing path.
Implement one of:
- secondary search provider fallback, or
- lightweight search endpoint + model summarization, or
- alternate OpenAI web-search profile with lower output/search context.

Goal:
“What is the news today?”
must return live sources, not fallback text, unless all search providers fail.

3. Mobile Retry parity bug
Website Retry sends forceSearch:true.
Mobile Retry does not.

Fix mobile:
- Add forceSearch?: boolean to artifacts/ora-mobile/lib/types.ts ChatRequest.
- Ensure sendChat passes forceSearch through.
- In artifacts/ora-mobile/app/(home)/index.tsx, when pressing Retry live search on a message with searchFallback/searchRetryable, call sendMessage with forceSearch:true.
- Add tests proving mobile Retry includes forceSearch:true.

4. Verify both platforms
Run signed-in tests on website and mobile:
- “What is the news today?”
- “Latest AI news today”
- “Current stock market news”
- “What happened today in tech?”

Expected:
- routedTool/searchUsed = search
- sources.length > 0
- searchFallback = false
- fallbackReason = null

If searchFallback is true:
- Retry live search must run forceSearch:true
- It must not repeat the same general-knowledge fallback unless all search attempts fail

5. Do not submit TestFlight until:
- Website live search works
- Mobile Retry sends forceSearch:true
- Mobile build includes the fix
- Manual QA confirms Retry actually produces live sourced results