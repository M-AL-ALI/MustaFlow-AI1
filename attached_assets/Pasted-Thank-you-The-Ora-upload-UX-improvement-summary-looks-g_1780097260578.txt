Thank you. The Ora upload UX improvement summary looks good, but before I accept this task as complete, please provide a short validation report with proof.

Also, please include the Arabic voice recognition issue in the same validation/fix report.

Do not start Phase 7, Phase 8, Phase 9, thread scrolling, image generation, Knowledge Vault, Web Search, Advanced Charts, or any unrelated roadmap work under this task.

This task should stay focused on:

1. Ora upload UX validation:

* visible upload button
* drag-and-drop upload
* paste-to-upload
* existing upload behavior still working

2. Arabic voice recognition validation/fix:

* Arabic dictation / Talk with Ora transcript accuracy
* RTL text display
* Arabic voice fallback behavior if browser-native recognition is weak

Part A — Upload UX validation

1. Final affected files

List every file modified.

2. Final commit hash

Confirm the commit hash for the upload UX improvement.

3. Upload button proof

Confirm:

* visible upload button exists in Ora Panel
* visible upload button exists in Ora Bubble
* aria-label is now Upload image or file
* tooltip/title lists supported types: PNG, JPG, WEBP, PDF, DOCX, TXT, CSV, XLSX
* mobile file picker still opens normally

4. Drag-and-drop proof

Confirm:

* drag over Ora Panel shows overlay
* drag over Ora Bubble shows overlay
* overlay says “Drop image or file to upload”
* dropping PNG/JPG/WEBP uploads through existing flow
* dropping PDF/DOCX/TXT uploads through existing flow
* dropping CSV/XLSX uploads through existing flow
* unsupported dropped file shows safe error
* oversized dropped file shows safe error
* if attachment already exists, dropping another file shows “Remove the current attachment before uploading another.”
* drop error auto-clears safely

5. Paste upload proof

Confirm:

* pasting an image into Ora Panel textarea uploads image
* pasting an image into Ora Bubble textarea uploads image
* pasted image creates image thumbnail chip
* normal text paste still works normally
* unsupported pasted file shows safe error if browser provides file item
* paste does not break typing, dictation transcript, or send behavior

6. Existing upload behavior proof

Confirm:

* image thumbnail chip still works
* document file chip still works
* dataset chip still works
* upload state works
* attached state works
* error state works
* remove attachment works
* failed upload keeps UI stable
* failed analysis still keeps chip for retry/remove if that was existing behavior

7. Backend/scope proof

Confirm:

* no new backend upload route was added
* all uploads still use POST /api/public-ai/upload
* existing upload validation still applies
* file/image/dataset limits still apply
* magic-byte validation still applies
* no permanent storage added
* no database changes
* no OpenAPI changes unless already required
* no Builder/project memory access
* no Knowledge Vault learning
* no billing/credit behavior

8. Upload privacy proof

Confirm logs do not contain:

* file contents
* image bytes
* base64
* pasted content
* user message text
* raw file contents
* private refs

Part B — Arabic voice recognition issue

Current issue:

Arabic language is not being captured correctly by Ora Voice Mode / Talk with Ora. The spoken Arabic transcript is inaccurate, and the written Arabic text produced from speech is not reliable.

Please investigate and fix or improve this.

This applies to:

* normal mic/dictation path
* Voice Conversation Mode / Talk with Ora
* any Whisper/backend transcription path already implemented

Important:

Do not silently produce bad Arabic text without giving the user a chance to review/edit.

9. Arabic voice expected behavior

When language is Arabic:

* speech recognition/transcription should use Arabic language settings
* Arabic transcript should appear in Arabic script correctly
* transcript should be right-to-left friendly in the composer
* transcript should be right-to-left friendly in the message thread
* interim transcript should display Arabic cleanly
* Arabic punctuation and spacing should be acceptable
* Ora should reply in Arabic when the user speaks Arabic
* if Arabic transcription is unreliable, user should be able to review/edit before sending
* if Voice Conversation Mode auto-sends after silence, Arabic should use a safer confirmation/review path if confidence is low or transcription quality is poor

10. Arabic language mapping

Confirm Arabic voice mode uses the correct language code, such as:

* ar-SA as default Arabic

Also consider fallback options if needed:

* ar
* ar-AE
* ar-EG
* browser-supported Arabic variants

Please confirm what language code is used in:

* browser-native SpeechRecognition
* Whisper/backend transcription if used
* browser-native speechSynthesis / TTS if used

11. Browser-native Arabic behavior

If browser SpeechRecognition is used:

* confirm whether the browser actually supports Arabic recognition
* confirm Arabic recognition is tested on the browser/device available
* if unsupported or inaccurate, show a friendly message:
    “Arabic voice recognition may depend on your browser/device. Please review the transcript before sending.”

Do not overpromise perfect Arabic voice recognition.

12. Whisper / backend Arabic behavior

If POST /api/public-ai/transcribe is used:

Confirm:

* gpt-4o-mini-transcribe receives an Arabic language hint if supported
* Arabic output is returned in Arabic script
* Arabic transcript is not logged
* audio is not stored
* audio is not written to disk
* rate limits still apply
* safe error is returned if transcription fails

13. RTL UI support

Arabic transcript display should be right-to-left friendly in:

* composer textarea
* message bubble
* interim transcript area
* Voice Conversation Mode live transcript area
* final thread messages

Please add or confirm use of appropriate direction handling, such as:

* dir="auto" where practical
* RTL-friendly alignment/wrapping
* no broken punctuation/layout with Arabic text

14. Review-before-send safety

For normal mic/dictation:

* Arabic transcript must land in composer first
* user can review/edit before sending
* Arabic dictation must not auto-send

For Voice Conversation Mode:

* if auto-send is enabled after silence, consider Arabic-specific safer behavior:
    * short review delay
    * confirmation step
    * or auto-send only when transcript is clear/reliable
* if Arabic transcript seems unclear, ask user to confirm instead of sending automatically

15. Arabic voice proof required

Please provide proof for:

* Arabic dictation transcript in composer
* Arabic Voice Conversation Mode transcript in message thread
* Ora response in Arabic
* Arabic text displays RTL correctly
* browser-native Arabic behavior confirmed or fallback chosen
* backend transcription Arabic behavior confirmed if used
* no Arabic transcript appears in logs
* no audio stored
* normal English voice behavior still works
* normal Spanish voice behavior still works

Suggested user-facing wording if Arabic recognition is limited:

“Arabic voice recognition depends on your browser and device. Please review the transcript before sending.”

Part C — Regression and quality validation

16. Regression proof

Confirm:

* Phase 1 chat still works
* Phase 2 document analysis still works
* Phase 3 dataset analysis still works
* Voice features still work
* Phase 5 image analysis still works
* Phase 6 handoff still works
* Task #1151 remains separate
* thread scrolling remains separate

17. Quality checks

Report:

* typecheck status
* lint status
* format status
* frontend workflow status
* backend workflow status
* any unrelated failures if present

18. Rollback plan

Explain how to remove drag/drop and paste upload behavior while keeping:

* normal upload button
* Phase 1 chat
* Phase 2 document analysis
* Phase 3 dataset analysis
* voice features
* Phase 5 image analysis
* Phase 6 handoff

Also explain how to roll back any Arabic voice adjustments if needed.

Final instruction:

Please provide one combined validation/fix report covering both:

1. Upload UX improvements
2. Arabic voice recognition / RTL transcript accuracy

After I review that report, I will decide whether this task is accepted as complete.