Wave 2A implementation is approved, but do not mark it fully closed until the Git/checkpoint issue is resolved and the final commit hash is confirmed.

Completed:

* ORA_DISABLED global kill switch
* feature-specific kill switches:
    * ORA_VOICE_DISABLED
    * ORA_FILE_UPLOAD_DISABLED
    * ORA_TRANSCRIBE_DISABLED
    * ORA_TTS_DISABLED
    * ORA_GENERATE_FILE_DISABLED
    * ORA_FILE_ANALYSIS_DISABLED
    * ORA_DATASET_ANALYSIS_DISABLED
    * ORA_IMAGE_ANALYSIS_DISABLED
    * ORA_STREAMING_DISABLED
    * ORA_WEB_SEARCH_DISABLED
* hot-swappable env checks without restart
* user-friendly structured 503 responses
* Ora isolation preserved
* asset retention scheduler added
* old ora_assets rows soft-delete after ORA_ASSETS_RETENTION_DAYS, default 90 days
* retention scheduler runs after boot and then every 24 hours
* timers are unref() so they do not hold the process open
* 50 new tests added
* quality gate passed: 1510 tests passing

Important:
The final report says the quality gate passed and changes were pushed to GitHub, but it also says the checkpoint commit is blocked by git exit code 128. Please resolve or verify this before closing.

Required before full acceptance:

1. Confirm the git workspace is clean.
2. Confirm the checkpoint commit succeeded.
3. Confirm the changes are pushed to GitHub.
4. Provide the final commit hash.
5. Confirm the quality gate still passes after the final commit/push.

Acceptance:
Wave 2A is fully closed only after the final commit hash is confirmed.

After that, move to Wave 2B:

Ora Production Safety Wave 2B — Monitoring + Admin Visibility

Goal:
Give admins visibility into Ora production behavior.

Wave 2B requirements:

1. Admin spend summary endpoint.
2. Daily units used globally.
3. Daily units by feature.
4. Daily units by user/tier, safely aggregated.
5. Daily spend cap threshold events.
6. Provider fallback summary:
    * provider failed
    * fallback used
    * all providers failed
    * error category
7. High 429-rate summary.
8. Ledger degraded / quota fail-open summary.
9. Admin-only access control.
10. Tests for:

* admin can view summary
* non-admin is blocked
* no raw user prompts
* no raw file text
* no secrets
* no provider stack traces
* no AI Builder / Builder / handoff language

Keep Ora fully isolated:
No AI Builder.
No Builder.
No handoff.
No shared flow.