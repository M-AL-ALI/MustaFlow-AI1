Good Phase 3A report. Do NOT publish yet.

I agree with locking mobile generation out for now. MustaFlow AI should focus on static/generated web apps first until the web builder, export, security, and publishing pipeline are strong.

Please start Phase 3B: Real Web Product Readiness.

The goal of Phase 3B is to make MustaFlow AI safe and useful for real web-app users before adding mobile generation.

==================================================
PHASE 3B PRIORITY ORDER
==================================================

1. Real secrets encryption
2. Secret audit log + dev/staging/prod namespaces
3. Delete project
4. Rename project / edit project details
5. True public publishing
6. Knowledge Vault auto-population
7. API unknown route behavior cleanup
8. Final validation report

Do not publish production.
Do not enable mobile generation.
Do not store real production secrets until encryption is active.

==================================================
TASK 1 — REAL SECRETS ENCRYPTION
==================================================

Current issue:
Secrets are still stored plaintext using DevOnlyPassthroughEncryption.

This must be fixed before any real user stores API keys.

Implement real encryption at rest.

Requirements:
- Use AES-256-GCM or equivalent secure encryption.
- Use ENCRYPTION_KEY from Replit Secrets/environment.
- Do not start production mode if ENCRYPTION_KEY is missing.
- Encrypt secret values before storing in DB.
- Decrypt only inside server-side trusted integration logic.
- Never return raw secrets in API responses.
- Continue returning masked values only.
- Keep value_encrypted column but make the value actually encrypted.
- Add safe migration handling for old dev plaintext secrets.

Acceptance:
- Create a secret.
- Confirm DB value is encrypted and not readable plaintext.
- GET secrets returns masked value only.
- Export ZIP does not include raw secrets.
- Typecheck passes.

==================================================
TASK 2 — SECRET AUDIT LOG + ENVIRONMENT NAMESPACES
==================================================

Add real secret audit logging and stronger environment separation.

Secret environments:
- development
- testing
- staging
- production

Secret categories:
- ai
- auth
- payment
- maps
- database
- storage
- email
- sms
- deployment
- analytics
- monitoring
- other

Audit log every:
- create
- update
- delete
- verify
- failed verification

Audit log fields:
- id
- projectId
- secretId
- actorUserId
- action
- environment
- category
- metadata
- createdAt

Rules:
- Do not allow production publish if required production secrets are missing.
- Warn if a test key is added under production.
- Warn if a production-looking key is added under testing.
- Never expose raw secret values.

Acceptance:
- Secret create creates audit log.
- Secret update creates audit log.
- Secret delete creates audit log.
- Environment separation appears in UI.
- Production publish checks production secrets only.

==================================================
TASK 3 — DELETE PROJECT
==================================================

Enable Delete Project in Manage tab.

Requirements:
- User must confirm before deletion.
- Project owner only.
- Delete or soft-delete project safely.
- Soft delete is preferred.
- Deleted project should disappear from dashboard.
- Project files, secrets, tasks, versions, knowledge entries, and deployments should be handled safely.
- Do not allow deleting another user's project.

Acceptance:
- Delete button works.
- Confirmation required.
- Project disappears from dashboard.
- API returns JSON success.
- Another user cannot delete the project.

==================================================
TASK 4 — RENAME PROJECT / EDIT PROJECT DETAILS
==================================================

Users need to change the auto-generated project name.

Add project settings/edit form.

Fields:
- project name
- description
- project type/kind
- status display
- summary if available

Requirements:
- Owner only.
- Save changes.
- Update dashboard card.
- Update workspace header.
- Add audit/event entry if audit system exists.

Acceptance:
- Rename project works.
- Description update works.
- Dashboard reflects changes.
- Workspace reflects changes.

==================================================
TASK 5 — TRUE PUBLIC WEB PUBLISHING
==================================================

Current issue:
Publish only sets status = published and returns an auth-protected preview URL.

That is not true public publishing.

Implement real public publishing for generated static web apps.

Minimum acceptable v1:
- Create public route that serves published generated files without requiring login.
- Published apps should be accessible by public slug or public project ID.
- Public route must serve the latest published snapshot, not live draft files.
- Draft changes should not affect public site until user publishes again.
- Unpublish should disable public access.
- Published snapshot should not expose secrets.
- Public page should not expose MustaFlow internal project data.

Preferred route examples:
- /p/:publicSlug
- /published/:publicSlug
- /site/:publicSlug

Publishing flow:
- User clicks Publish Testing or Publish Production.
- System creates deployment record.
- System creates published snapshot.
- System saves deployment logs.
- System returns public URL.
- Public URL opens without auth.
- Unpublish disables it.
- Republish updates the published snapshot.

Before production publish require:
- confirmation
- checklist pass
- required files exist
- no raw secrets
- rollback point
- deployment log

Acceptance:
- Publish creates public URL.
- Public URL opens in incognito/signed-out browser.
- Public URL shows generated app.
- Draft changes do not change public URL until republished.
- Unpublish removes public access.
- Deployment logs saved.

==================================================
TASK 6 — KNOWLEDGE VAULT AUTO-POPULATION
==================================================

Current issue:
Knowledge Vault UI exists, but auto-population from build summaries is incomplete.

Fix this.

Every important event should save a Knowledge Vault entry:
- app blueprint created
- files generated
- change request completed
- bug fixed
- rollback performed
- publish completed
- publish failed
- integration required
- secret warning
- test report created

Fields:
- projectId
- userId
- type
- title
- summary
- details
- relatedTaskId
- relatedVersionId
- tags
- severity
- approvedForReuse
- createdAt

Rules:
- Default approvedForReuse = false.
- Do not create uncontrolled global learning from private project data.
- Project-level memory is allowed.
- Reusable lesson must require approval.

Acceptance:
- Build task creates Knowledge Vault entry.
- Change request creates Knowledge Vault entry.
- Rollback creates Knowledge Vault entry.
- Publish creates Knowledge Vault entry.
- UI displays entries.

==================================================
TASK 7 — API UNKNOWN ROUTE BEHAVIOR
==================================================

Current behavior:
- Unauthenticated unknown route returns JSON 401.
- Authenticated unknown route returns JSON 404.

This is at least JSON, which is good, but I want clean API behavior.

Please review route order and confirm the intended behavior.

Preferred:
- Unknown API route should return JSON 404 when the route truly does not exist.
- Protected known routes should return 401/403 when auth/ownership fails.
- Never return HTML for API errors.

Acceptance:
- /api/healthz returns JSON 200.
- Known protected route signed out returns JSON 401.
- Known protected route wrong owner returns JSON 403.
- Unknown API route returns JSON 404.
- No API route returns HTML error.

==================================================
TASK 8 — FINAL VALIDATION REPORT
==================================================

After Phase 3B, provide a full proof report.

Grade each item PASS / PARTIAL / FAIL:

1. Real secrets encryption
2. Secret masking
3. Secret audit log
4. Dev/testing/staging/production secret separation
5. Delete project
6. Rename project
7. True public publishing
8. Published URL works signed out
9. Published snapshot separated from draft
10. Unpublish works
11. Knowledge Vault auto-populates
12. API JSON errors
13. Auth still works
14. Ownership still works
15. Export ZIP still works
16. Duplicate project still works
17. Builder still generates files
18. Preview still works
19. Version history still works
20. Rollback still works
21. Typecheck passes
22. API health passes
23. No mobile generation re-enabled

Also include:
- files changed
- backend routes added/changed
- schema changes
- test results
- screenshots or clear descriptions
- known limitations
- next phase recommendation

Do not publish production.
Do not enable mobile generation.
Wait for my review.