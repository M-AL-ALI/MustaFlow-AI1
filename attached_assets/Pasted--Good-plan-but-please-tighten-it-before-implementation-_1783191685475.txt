“Good plan, but please tighten it before implementation. The goal is not only first token; Instant mode must feel fast for the whole short answer across authenticated website Ora, landing-page Ask Ora bubble, and Ora Mobile. Do not touch or slow down the existing word-by-word streaming animation/cadence. Because the cadence is staying unchanged, the only way to make full Instant replies consistently fast is to make Instant replies shorter and route them through a truly fast model. For simple Instant prompts, enforce a concise response budget: direct answer first, usually 1–3 short sentences, no long preamble, no unnecessary bullets, no deep reasoning unless the user asks. If the answer is long, full completion under 2 seconds is impossible without changing cadence, so Instant mode should intentionally produce short useful answers and offer ‘I can expand if you want.’
Please update the task with these corrections:
Acceptance criteria must separate first token, first useful sentence, and complete short answer
First visible word target: p50 under 800ms, p95 under 1.5s on healthy network.
First useful sentence target: p50 under 1.2s, p95 under 2s.
Complete short Instant answer target: p50 under 2s, p95 under 3s.
Do not claim ‘always under 1 second’ because network/provider variance makes that unrealistic. Instead, log every miss and identify the slow bucket.

Instant mode must use a short-answer budget
For simple Instant prompts, cap output to a concise answer unless the user asks for details.
Suggested default: about 60–90 output tokens or 1–3 short sentences.
Deep mode can keep longer, richer answers.
Research, file/image/video analysis, and complex reasoning are excluded from this speed SLA.

Do not run the async classifier at all for fast-lane
The plan says this, and it is important.
For deterministic fast-lane prompts, skip classifier completely.
Do not launch classifier in the background if it wastes model capacity.
Use deterministic gates only: no attachment, no image/file/video/research cue, not Deep mode, not explicit memory/history request.

Light context only
For Instant fast-lane, use only very recent conversation context, ideally the last 2–3 turns.
Skip full memory/history/context unless the prompt explicitly asks about memory, history, a prior conversation, or user-specific facts.
If context is not ready immediately, answer without blocking.

Model benchmark must drive the routing
If current fast model still has 1.0–1.7s server TTFT, it will not meet the target.
Benchmark every available fast candidate through the real /api/public-ai/chat/stream route.
Choose the fastest stable model for Instant fast-lane.
Keep higher-quality/slower models for Deep, research, files, images, and complex analysis.

Find and eliminate silent /chat usage
The plan is correct that /api/public-ai/chat should not be hit for normal Instant chat.
Identify which surface still calls it: website, landing bubble, mobile, or fallback.
Normal Instant chat must use /chat/stream first.
/chat should only be fallback with a logged fallback reason.

Diagnostics must be production-grade
Add shared requestId.
Add surface: web-authenticated, web-landing, mobile.
Log: model/provider, route tier, fast-lane true/false, classifier skipped true/false, fallback true/false, first token, first sentence, completion time, token count, tokens/sec.
Mobile Settings diagnostics should show the last response timing and fallback reason.

Include all three surfaces
Website /ora
Landing-page Ask Ora bubble
Ora Mobile TestFlight
Do not optimize one and assume the others are fixed.

Correct the file paths before editing
Mobile is likely artifacts/ora-mobile/app/(home)/index.tsx, not artifacts/ora-mobile/app/(home)/ora.tsx.
Web bubble may be artifacts/mustaflow/src/components/ora-bubble.tsx, not artifacts/mustaflow/src/components/ora/ora-bubble.tsx.
Please verify actual paths before editing.

Final verification
Run at least 20 simple Instant prompts on all three surfaces.
Report p50 and p95 for first token, first sentence, and complete short answer.
Confirm no word-by-word streaming regression.
Confirm no Deep/research/file/image/video quality regression.
Confirm Ora Mobile TestFlight uses streaming and does not silently fall back.

Most important correction: because we are not changing word-by-word streaming cadence, full Instant speed depends heavily on shorter Instant answers + faster model routing + no classifier/context blocking. If they only add diagnostics and benchmark logs, the user complaint will remain.”