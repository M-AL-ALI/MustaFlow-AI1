Fix the uploaded-file edit workflow properly. The latest publish still fails.

Observed production issue:
- User uploads a DOCX.
- User asks:
  “Make it Professional, Add a section called Risk Notes with content Track renewal risk and return the Word document.”
- Ora still replies with text instead of reliably returning a real edited DOCX.
- Sometimes Ora creates a different/new file instead of editing the uploaded file.
- The returned file does not preserve the original layout/UI/style.
- Sometimes an unrelated image-generation CTA appears.

This must be fixed for both website and mobile through the shared backend.

Required behavior:
Uploaded file edits must edit the original uploaded file package whenever possible. They must not silently fall back to generic text answers or regenerated unrelated files.

Implementation requirements:

1. Verify frontend payloads
- On website and mobile, after DOCX/PPTX/XLSX/PDF upload, every follow-up chat request must send `documentRefs`.
- Verify in network/server logs that `/api/public-ai/chat` receives `documentRefs: [...]`.
- If documentRefs are missing, fix client state so uploaded file refs persist through:
  - normal send
  - stream fallback
  - retry
  - mobile send path

2. Hard backend routing rule
If:
- `documentRefs.length > 0`
- user asks to edit/revise/professionalize/replace/delete/add/return/save/send the uploaded file

Then:
- force `decision.tool = "file_generation"`
- infer output format from the uploaded file first
- keep same type by default:
  - uploaded DOCX → edited DOCX
  - uploaded PPTX → edited PPTX
  - uploaded XLSX → edited XLSX
  - uploaded PDF → edited PDF if supported, otherwise fail honestly
- do this before image CTA, answer, search, or normal chat
- never show image-generation CTA for uploaded document edits

3. Mandatory layout-preserving edit for Office files
For uploaded DOCX/PPTX/XLSX:
- Use the original uploaded raw Office package.
- Apply edits inside the original package.
- Preserve original layout, styles, fonts, spacing, slides, tables, sheets, formulas, and theme as much as possible.
- Return the edited original file type unless the user explicitly asks to convert.

4. No silent fallback to regenerated files
If layout-preserving edit fails for an uploaded-file edit:
- Do not silently generate a brand-new file.
- Do not paste a fake “revised file” as chat text.
- Do not claim success without a downloadable file.

Instead return a clear recoverable message:
“I could not safely edit the original file layout. Please try a more specific edit or ask me to create a new version.”

Only regenerate/redesign a new file if the user explicitly asks:
- “create a new version”
- “redesign it”
- “make a new layout”
- “create from scratch”

5. Add regression tests
Add automated tests for:
- DOCX upload + “Make it Professional, Add a section called Risk Notes with content Track renewal risk and return the Word document” routes to file_generation.
- Response returns a downloadable DOCX, not text-only.
- No image-generation CTA appears.
- DOCX result contains clean “Risk Notes”.
- DOCX result contains clean “Track renewal risk”.
- DOCX result does not contain junk wording like “called Risk Notes” or “content Track renewal risk”.
- When layout-preserving edit fails, Ora does not fake success and does not regenerate unrelated layout unless explicitly requested.
- Website and mobile both send documentRefs correctly after upload.

6. Update Ora stability gate coverage
Add this uploaded-file-edit scenario to the stability gate so every future release checks it:
- Upload DOCX
- Ask to professionalize it and add Risk Notes
- Confirm route goes to file_generation
- Confirm response returns downloadable DOCX
- Confirm no image-generation CTA
- Confirm no silent fallback to regenerated unrelated layout

7. Run mandatory release QA gate
Do not publish until this passes cleanly:

pnpm --filter @workspace/scripts run ora-stability-gate -- --profile=release --require-clean --report=tmp/ora-stability-gate-report.md

Required:
- 0 failed
- 0 warnings
- feature registry passes
- api-server typecheck passes
- website typecheck passes
- mobile typecheck passes
- Ora routing tests pass
- file/chart/export tests pass
- website/mobile parity checks pass

If any gate fails, do not publish and do not submit TestFlight.

8. After publish, run production smoke
Use this exact production prompt after uploading a DOCX:

“Make it Professional, Add a section called Risk Notes with content Track renewal risk and return the Word document.”

Pass criteria:
- Real downloadable DOCX card/file appears.
- No unrelated image-generation CTA.
- Ora does not only paste revised text in chat.
- Edited DOCX contains “Risk Notes”.
- Edited DOCX contains “Track renewal risk”.
- Original formatting/layout is preserved as much as possible.
- Ora does not silently create a completely different layout unless user explicitly asked for redesign/new version.

Important:
Keep the fix scoped to Ora uploaded-file edit workflow. Do not touch unrelated fixed areas like Talk to Ora, search, billing, image generation, account sync, App Store compliance, or ORAX unless a test proves they are directly involved.