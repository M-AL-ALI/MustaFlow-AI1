Please fix the Ora audit findings and push the changes.

Scope:

1. Persist full Ora message UI state
- Update backend schemas in:
  - artifacts/api-server/src/routes/ora-transcript.ts
  - artifacts/api-server/src/routes/ora-conversations.ts
- They must preserve these OraMessage fields:
  - sources
  - imageUrl
  - imageId
  - editInstruction
  - memorySaveCandidate
  - memorySaveCandidateConfidence
  - memorySaved
  - generatedFile where appropriate
- Important: do not keep huge/generated file base64 in conversation storage unless that is intentional. If fileData is stripped, the frontend must not render a broken download card after reload. Prefer storing asset/library reference metadata instead.

2. Fix signed-in upload gating
- In artifacts/mustaflow/src/hooks/use-ora-chat.ts, do not block signed-in users based on anonymous session file/image upload counters.
- Backend already treats uploads as unlimited for signed-in users, so frontend must match that behavior.

3. Refund Ora image quota when async inline image edits fail
- In artifacts/api-server/src/lib/image-generation-jobs.ts and/or the image edit enqueue path, ensure Ora-origin edits using billingMode: "ora" refund daily image quota if the background edit job fails after enqueue.
- Existing credit refund logic only covers Builder credits; Ora quota needs its own refund path.

4. Add/preserve sensitive-memory guard
- Ensure explicit memory candidates containing sensitive facts like phone numbers, emails, addresses, API keys, tokens, SSNs, payment/bank info, passwords, or private credentials are never auto-saved.
- They should be downgraded to low confidence and shown only as manual-save suggestions with a warning.
- Add tests for this.

5. Persist Ora edited images into the Ora Library
- Ora-origin image edits should be saved into ora_assets after successful edit completion, same as generated Ora images/uploads.
- Best-effort persistence is fine; it should not break the chat response/job if library persistence fails.

6. Tests/verification
- Add or update tests for:
  - Ora message persistence preserving citations/images/memory fields.
  - Signed-in uploads not blocked by anonymous counters.
  - Ora image edit quota refund on async failure.
  - Sensitive memory downgrade/no auto-save.
  - Edited images copied into Ora Library.
- Run typecheck, lint, format, and focused Ora tests.
- Also investigate why Ora-focused Vitest commands hang locally if reproducible.

After fixing, please commit and push the branch to GitHub/Replit remote.