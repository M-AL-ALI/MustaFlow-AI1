Good Phase 4B report. I accept the domain foundation for internal testing.

Do NOT publish production yet.
Do NOT enable mobile generation yet.

Start Phase 5A: Production Launch Hardening.

Main priorities:

1. Implement real SSL automation using Cloudflare for SaaS.
2. Refactor publish/unpublish routes into a clear publish.ts route file.
3. Add Admin RBAC so only admin users can access /admin.
4. Add Stripe/build-credit top-up flow.
5. Add final production launch checklist and validation report.

==================================================
TASK 1 — REAL SSL AUTOMATION WITH CLOUDFLARE FOR SAAS
==================================================

Implement Cloudflare for SaaS integration for custom domains.

Required env vars:
- CF_ZONE_ID
- CF_API_TOKEN
- PLATFORM_DOMAIN
- PLATFORM_CNAME_TARGET

Database:
Add columns if needed:
- cf_hostname_id
- ssl_status
- ssl_error
- ssl_verified_at
- domain_verified_at

Flow:
1. User saves custom domain.
2. User verifies TXT or CNAME.
3. After domain ownership/DNS verification passes, call Cloudflare API to create Custom Hostname.
4. Store Cloudflare custom hostname ID.
5. Set sslStatus = provisioning.
6. Poll Cloudflare or provide webhook endpoint to update sslStatus.
7. When Cloudflare reports active SSL, set sslStatus = active.
8. If failed, show sslStatus = failed with error message.

Add endpoint:
POST /api/projects/:id/domain/ssl-activate

Optional webhook:
POST /api/domain/ssl-webhook

UI:
- Show SSL pending
- Show SSL provisioning
- Show SSL active
- Show SSL failed
- Show clear errors
- Do not mark custom domain production-ready until sslStatus = active

Acceptance:
- Domain can be DNS verified.
- SSL activation endpoint calls Cloudflare or cleanly reports missing Cloudflare env vars.
- SSL status updates are saved.
- UI does not falsely say SSL is active.
- Public custom domain is only marked fully live when SSL is active.

==================================================
TASK 2 — REFACTOR PUBLISH ROUTES
==================================================

The publish handler should not live in duplicate.ts.

Move publish/unpublish logic into a clear file:

artifacts/api-server/src/routes/publish.ts

Required:
- POST /api/projects/:id/publish
- POST /api/projects/:id/unpublish

Keep behavior:
- Generate public slug on first publish
- Freeze current files into snapshot
- Save published_snapshot_id
- Save deployment log
- Save Knowledge Vault entry
- Return public URL
- Unpublish disables public access
- Republish updates snapshot

Acceptance:
- Publish still works after refactor.
- Unpublish still works.
- Public slug still works.
- Domain/subdomain routing still works.
- Typecheck passes.

==================================================
TASK 3 — ADMIN RBAC
==================================================

Current issue:
Admin page is auth-gated but any signed-in user can access it.

Fix:
- Add user role system.
- Roles: user, admin, owner
- Add requireAdmin middleware.
- Protect /admin frontend route.
- Protect all /api/admin routes.
- Non-admin users must receive JSON 403 from API.
- Non-admin users must not see admin page.

Acceptance:
- Admin user can access /admin.
- Normal user cannot access /admin.
- API returns JSON 403 for non-admin.
- Admin dashboard still loads for admin.

==================================================
TASK 4 — STRIPE / BUILD CREDIT TOP-UP
==================================================

Build credits are enforced, but users need a way to buy more.

Add Stripe checkout placeholder or real Stripe if keys exist.

Required:
- Billing page shows credit balance.
- User can choose credit package.
- Create checkout session endpoint.
- Stripe webhook placeholder for payment success.
- Credit transaction created after successful purchase.
- If Stripe keys are missing, show setup-required state.

Credit packages example:
- Starter credits
- Builder credits
- Power credits

Acceptance:
- Not enough credits blocks AI build.
- Billing page explains why.
- Checkout endpoint works or returns clear setup-required response.
- Credit transactions are recorded.

==================================================
TASK 5 — FINAL PRODUCTION LAUNCH CHECKLIST
==================================================

Add a launch readiness page or panel.

Checklist:
- Auth active
- Admin RBAC active
- AES encryption active
- ENCRYPTION_KEY present
- Stripe configured
- Cloudflare SSL configured
- PLATFORM_DOMAIN configured
- PLATFORM_CNAME_TARGET configured
- Public slug publishing works
- Custom domain SSL works
- Rate limits active
- Terms/privacy/help pages exist
- Mobile generation disabled
- No plaintext secrets
- Unknown API routes return JSON
- Typecheck/build passes

Acceptance:
- Launch checklist exists.
- Failed items block production launch.
- Each item shows pass/fail/partial.

==================================================
FINAL PHASE 5A REPORT REQUIRED
==================================================

After Phase 5A, provide PASS / PARTIAL / FAIL for:

1. Cloudflare SSL integration
2. SSL activation endpoint
3. SSL status polling/webhook
4. Publish route refactor
5. Publish still works
6. Unpublish still works
7. Public slug still works
8. Custom domain still works
9. Admin RBAC
10. Non-admin blocked from admin
11. Stripe/build credit top-up
12. Credit enforcement still works
13. Launch checklist
14. Auth still works
15. Ownership still works
16. AES encryption still works
17. Export still works
18. Duplicate still works
19. Builder still works
20. Preview still works
21. Typecheck passes
22. API health passes
23. Unknown API route returns JSON 404
24. Mobile generation still disabled

Do not publish production.
Wait for my review.