STOP claiming Ora Mobile matches website Ora.

The TestFlight still does not visually match. The user tested it on iPhone and the result is not acceptable.

Requirement:
Ora website = Ora mobile.

This means:
- Same Ora identity
- Same logo/atom treatment
- Same background
- Same theme
- Same colors
- Same layout language
- Same spacing
- Same message bubbles
- Same composer
- Same icons
- Same empty state
- Same drawers/panels
- Same plan colors
- Same feature behavior
- Same backend logic

Do not describe it as "native equivalent" unless the visual result is approved.

NEW PROCESS REQUIRED:

1. Stop small patches.
2. Create a visual parity board/report before more coding.
3. For each screen, place:
   - Website Ora screenshot
   - Current TestFlight screenshot
   - Difference list
   - Required fix
   - Source files used
4. Fix screen by screen until the screenshots match.

Screens to compare:
- Main empty chat
- Chat with user + assistant messages
- Composer focused with keyboard
- Plus/tools menu
- Mic dictation state
- Talk to Ora state
- Conversation/history drawer
- Projects drawer
- Memory screen
- Library screen
- Settings screen
- Help/support screen
- ORAX screen
- Light mode
- Dark mode
- Free plan
- Core Pack plan
- Deep Wave plan

Acceptance rule:
No more "it is similar."
No more "code confirms parity."
No more "native equivalent" unless approved.

The user must be able to look at website Ora and Ora Mobile and say:
"This is the same Ora product."

If React Native cannot realistically match the website UI fast enough, propose one of these options clearly:

Option A — Native parity rebuild:
Rebuild every Ora mobile component using React Native, copying website tokens, layout, spacing, colors, icons, and states. This is the preferred long-term path, but it must be done screen by screen with screenshots.

Option B — WebView parity shell:
Embed the actual website Ora mobile-responsive experience inside a native shell for immediate visual parity, while keeping native auth/session/deep-link handling where needed. This gives fastest "Ora website = Ora mobile" visual match, but may limit native mic/file/TTS behavior and must be reviewed for App Store quality.

Do not continue guessing. First produce the side-by-side visual gap report, then fix the gaps.