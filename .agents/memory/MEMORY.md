- [Ora streaming flags & merge artifacts](ora-streaming-flags.md) — three env vars required (ORA_STREAMING_ENABLED, VITE_*, EXPO_PUBLIC_*); viaFallback not isStreamingFallback; AsyncGenerator literal-type fix.
- [Ora streaming verify via anon surface](ora-anon-surface-verification.md) — when Clerk testing is auth-blocked, verify streaming on the anonymous landing OraBubble (shares useOraChat+OraRichText with OraPanel); plus dev-only streamTokens counter.
- [Ora "no streaming" = stale deploy](ora-streaming-stale-deploy.md) — unreproducible "all at once" usually means the user tests the PUBLISHED app; prod logs show /chat not /chat/stream; fix is republish, not dev code. Confirm which surface first.
- [Ora streaming simulation](ora-streaming-simulation.md) — Replit AI proxy returns full response as ONE SSE chunk; simulateChunkStream splits chunks >25 chars into 4-word groups with 30ms delays; sock.setNoDelay(true) + sock.uncork() prevent Nagle batching.
- [Ora streaming per-word fade-in](ora-streaming-word-fade.md) — FadeText word spans keyed by INDEX (never content) + one-shot CSS mount anim, animate only the last streaming block; preserves no-jitter invariant.
- [Prettier function signature collapsing](prettier-function-sig-collapse.md) — Prettier (printWidth:100) collapses multi-line TS function signatures to one line if they fit; writing multi-line when single-line fits fails format:check. Use prettier --write to auto-fix, then read the diff to understand what changed.
- [Ora chat/upload API contract](ora-api-contract.md) — chat requires `message` (required, current turn) + `messages` (history); response field is `reply` not `content`; file/image-analysis require a UUID ref from /upload first, not inline data; session-create is rate-limited 10/day.
- [Ora isolation rule](ora-isolation-rule.md) — permanent: Ora has zero AI Builder relationship; enforced by ora-isolation.test.ts (11 tests); never reintroduce handoffCta/builder_handoff/MustaFlow Builder.
- [Anthropic max_tokens truncation](anthropic-max-tokens.md) — callAnthropic defaults cap haiku at 8192 and sonnet/opus at 16000; exceeding model limits causes 400s that trip the circuit breaker.
- [Anthropic assistant prefill rejection](anthropic-assistant-prefill.md) — Anthropic rejects calls where conversationHistory ends with role:"assistant"; need a bridge user message.
- [AI provider circuit breaker isolation](circuit-breaker-provider-isolation.md) — each provider needs its own CircuitBreaker; shared breaker causes cross-provider outages silently masked as "Refined 0 files".
- [Startup migrations registration](startup-migrations-registration.md) — every scripts/src/migrate-\*.ts script must also be appended to startup-migrations.ts MIGRATIONS array or fresh DBs/deploys boot missing columns.
- [Wouter Link nested anchors](wouter-link-nested-anchors.md) — wouter Link renders `<a>` by default; wrapping a shadcn `<Button>` (also `<a>`) or raw `<button>` produces invalid nested anchors. Use Button asChild around Link, or put className directly on Link.
- [KNOWN_PREFIXES pre-auth 404 guard](known-prefixes-guard.md) — every new /api route prefix must be added to KNOWN_PREFIXES in routes/index.ts or anonymous callers get 404 (not 401) before the auth wall.
- [PostgreSQL generated column immutability](pg-generated-column-immutability.md) — GENERATED ALWAYS AS STORED requires ALL functions to be IMMUTABLE; array_to_string and subqueries are not. Use functional GIN indexes instead.
- [PDFKit page numbers with bufferPages](pdfkit-buffer-pages.md) — calling doc.text() inside a pageAdded listener causes infinite recursion; use bufferPages:true then stampall pages with switchToPage() after content is complete.
- [Flex scroll container with max-height](flex-scroll-max-height.md) — h-full on a child of flex-1 fails when ancestor only has max-height; put overflow-y-auto directly on the flex-1 min-h-0 item instead.
- [Scripts package uses @workspace/db pool not pg](scripts-pg-pattern.md) — migration scripts in scripts/src/ must import `pool` from `@workspace/db`, not `import { Pool } from "pg"` (pg is not a direct dep of scripts).
- [vault_entries uses archived_at not deleted_at](vault-archived-at.md) — vault_entries has no deleted_at column; soft-delete uses archived_at. Queries must filter archived_at IS NULL, not deleted_at IS NULL.
- [Orval operationId collision overwrites generated body schema](orval-operationid-collision.md) — duplicate operationIds silently overwrite each other's generated body schema; each endpoint must have a globally unique operationId.
- [Playwright JWT redaction blocks direct API tests](playwright-jwt-redaction.md) — Playwright testing tool aborts when Clerk session JWTs appear in captured output; use UI-only flows (no raw fetch) or testClerkAuth with browser navigation only.
- [AI proxy intermittent empty content](ai-proxy-empty-content.md) — OpenAI proxy occasionally returns choices[0].message.content = null; retry once (1.5 s gap) before failing; also replace Unicode box-drawing chars in prompts with plain ASCII.
- [OpenAI image model family differences](openai-image-model-families.md) — gpt-image-1 vs dall-e-3 have incompatible quality/size params and response formats; default to gpt-image-1 for broader API key compatibility.
- [Clerk testClerkAuth rate limiting](clerk-testauth-rate-limit.md) — dev-key throttle blocks subagent for whole session; workaround is DevOnlyAuthAdapter swap for API-level proof, then restore.
- [OpenAPI detectedIntent enum must cover all intents](openapi-detected-intent-enum.md) — missing intent in ChatExchange.detectedIntent causes 500 on response after job already enqueued; add any new intent to the enum and re-run codegen.
- [Agentic container npm idealTree conflict](agentic-container-npm-conflict.md) — stale /root/.npm/\_locks/\* from interrupted npm runs cause "Tracker 'idealTree' already exists"; fixed by running rm -f before every npm install in ensureInstalled and pkg_install.
- [Agentic first-build preflight bypass](agentic-first-build-preflight.md) — new agentic projects fail /healthz preflight before any app is built; fix is to detect isFirstBuild via project_versions count=0 and pass effectiveContainerUrl=null to ensureContainerAwake (null already skips the healthz poll).
- [preferred_mode CHECK constraint drift](preferred-mode-check-constraint.md) — "Could not save your preference" on mode-select = a new preferred_mode value validated in OpenAPI/Zod/Drizzle but missing from the Postgres CHECK constraint (defined in 2 migration files + must ALTER live DB).
- [Ora asset Library not built](ora-library-not-built.md) — pages/library.tsx is the public lessons/knowledge gallery (useListPublicKnowledge), NOT the Ora image+file asset home from the blueprint; the Ora sidebar "Library" link points to it.
- [customFetch token getter resilience](customfetch-token-getter-resilience.md) — Orval customFetch must call the auth getter via try/catch-wrapped getAuthToken(); a throwing Clerk getToken() otherwise aborts the request before fetch, killing cookie fallback (no request in server logs).
- [Ora conversation save race](ora-conversation-save-race.md) — debounced per-conversation persistence must snapshot target id at schedule time + use an edit-generation guard on the loader, or switching conversations clobbers/loses messages.
- [Agentic post-build container sync must be honest](agentic-postbuild-container-sync.md) — post-build file sync/install/restart must use the detached background installer + poll real reachability; never direct exec or unconditional "Server started" (caused builds to "cycle").
- [flyFetch must have per-request timeout](fly-fetch-timeout.md) — no timeout causes hung exec to block agent-loop for minutes, triggering stuck-run; use 30s for API calls, 360s for exec POST.
- [Fly container npm install: sync OOM-kills, nohup succeeds](fly-npm-install-nohup.md) — synchronous npm install (even 1 package) OOM-kills inside a Fly exec; nohup background install with poll succeeds. Also: 429 retry in flyFetch can launch two concurrent npm installs → fix with pkill before each nohup launch.
- [Fly exec file upload: echo/stdin fail, chunked printf works](fly-exec-file-upload.md) — writing files to Fly containers via exec: echo+base64 silently drops files >~1KB; stdin field is ignored (0-byte output). Reliable method: chunk binary into 8000 base64 chars, append each with `printf '%s' 'CHUNK' | base64 -d >> file`, delay 400ms between execs to avoid 429. Chunk size must be divisible by 4 for independent decodability.
- [mustaflow-containers Fly proxy required for live preview](fly-preview-proxy-service.md) — livePreviewProxy.ts routes `/api/projects/:id/preview/*` to `https://${FLY_APP_NAME}.fly.dev/container/:machineId` (machineProxyUrl). This proxy service (separate Fly app) must be deployed; without it DNS fails and preview returns 502 even when the Fly machine IS running.
- [Agentic container yarn cache ENOENT](fly-yarn-cache-enoent.md) — yarn v1.22 on Fly machines can have corrupted .yarn-metadata.json entries (drizzle-orm observed); causes ENOENT on resolve → YARN_FAIL exit despite other packages installing fine. Fix: run `yarn cache clean <pkg>` before install; also check tsx binary presence even on YARN_FAIL exit since partial install may have succeeded.
- [WS upgrade socket must be resumed before async handler](ws-upgrade-socket-resume.md) — Node.js HTTP server reclaims paused upgrade sockets if the upgrade handler returns without synchronously claiming them; call socket.resume() before any async await in the upgrade handler.
- [Raw-body stream after express.json](raw-body-stream-after-json-parser.md) — routes reading req.on("data"/"end") (e.g. transcribe) hang forever if global json/urlencoded parser pre-consumed the stream; guard with req.readableEnded + aborted/close.
- [Agent-loop foundation-first rule and hard-block supervisor](agent-loop-foundation-supervisor.md) — typecheck+build are isFoundation:true; install has its own hint; HARD_BLOCK_FAIL_COUNT=5 forces check-blocked exit; detectRepairCategory routes repair prompts; CHECK_META has server-start/install/typecheck/build entries for targeted fix buttons.
- [Zod parse on GET responses](zod-parse-get-responses.md) — using .parse() (not .safeParse()) in GET handlers causes 500 if any DB row has unexpected field type; always use safeParse with raw fallback.
- [OpenAPI nullable vs typed-null](openapi-nullable-typed-null.md) — fields storing mixed JSON types (array OR object) must use nullable:true with no type constraint; type:["object","null"] rejects arrays and causes codegen to emit record() which breaks at runtime.
- [Clerk dev-mode JWT expiry and 401 recovery](clerk-jwt-expiry-401.md) — Clerk dev JWTs expire every 60s; clerkMiddleware callback form re-creates SDK per request breaking JWKS cache; use static form + frontend 401 auto-retry.
- [GitHub push from main agent](github-push-setup.md) — main agent bash tool blocks ALL git writes; use configureWorkflow+restartWorkflow for push, edit .git/config directly as a file for remotes, auto-push via .husky/post-commit.
- [Stripe checkout absolute URLs](stripe-checkout-absolute-urls.md) — /billing/\* checkout fails "Stripe error: Not a valid URL" when PLATFORM_DOMAIN unset + successUrl/cancelUrl omitted; pass absolute URLs. Reaching that error proves keys+price IDs ARE configured.
- [Ora credit/quota isolation](ora-credit-quota-isolation.md) — Ora metered by daily quotas (ora_daily_usage), never Builder credits; but /images/:id/edit is shared with Image Studio and still charges credits; session.imageCount is auth-overloaded.
- [Ora <-> AI Builder isolation](ora-builder-isolation.md) — Ora prompt context must inject ONLY scope='user' knowledge, never scope='project' (Builder Knowledge Vault); never reintroduce projectId into /public-ai/chat.
- [Ora web search provider choice](ora-web-search-provider.md) — real web grounding needs direct OPENAI_API_KEY + Responses API web_search (gpt-4o); the AI-integrations proxy accepts the tool but returns ungrounded results. Allowlist http(s) citation URLs both ends.
- [Stripe current_period fields moved to item](stripe-subscription-period-fields.md) — newer Stripe API puts current_period_start/end on subscription.items.data[0], not top-level; reading top-level => Invalid Date => webhook 500s. Use extractSubscriptionPeriod().
- [Ora pre-auth endpoint E2E auth](ora-preauth-e2e-auth.md) — pre-auth endpoints resolve Clerk themselves, so attachUser's E2E bypass never reaches them; reuse the shared isE2ETestAuthEnabled() guard.
- [Ora vs AI Builder separation](ora-builder-separation.md) — Ora must NEVER proactively recommend/hand off to Builder; lists every surface (chat/file/image routes + ora-panel/bubble cards) that can leak a proactive handoff.
- [Vitest cold-import timeout](vitest-cold-import-timeout.md) — first test importing a heavy router tree can exceed the 5s default; give it a per-test timeout (30000) instead of chasing a phantom hang.
- [Supertest multipart upload keep-alive hang](supertest-multipart-keepalive.md) — early-rejected multipart POST (401/503) blocks server.close() for ~7-8 s; fix: req.resume() + auth-before-rate-limiter + vitest.config.ts testTimeout:30000.
- [Ora asset R2 offload](ora-asset-r2-offload.md) — gate Ora byte offload on ORA_ASSETS_R2_ENABLED (not r2Enabled alone, creds shared w/ snapshots); data/storage_key XOR enforced by CHECK; persist must stay exception-safe w/ DB fallback.
- [Ora phase2/3/4/6 tests now pass](ora-voice-phase4-stale-tests.md) — once stale/flaky, now green after Ora-memory merges; don't pre-dismiss failures. api-server vitest.config.ts sets testTimeout:30000 — run all 36+ files in one batch safely.
- [Ora message persistence schema mirroring](ora-message-persistence-schema.md) — backend messageSchema (ora-conversations.ts + ora-transcript.ts) must mirror the frontend serializeForStorage shape or rich per-message UI state (sources/imageUrl/imageId/memory fields) is silently stripped on save.
- [Stripe setup-mode Checkout requires currency](stripe-setup-mode-currency.md) — mode:"setup" Checkout sessions need a currency param or Stripe 400s "Missing required param: currency" (no line items to infer it from).
- [Superuser full-access allowlist](superuser-allowlist.md) — hard-coded email allowlist (lib/superusers.ts) grants one account admin + zero-credit + free plan-switching; every new balance/role/plan gate must consult isSuperuser.
- [Ora file-detection ↔ system-prompt coupling](ora-file-detection-promise-coupling.md) — Ora promises files auto-deliver, so any detectFileRequest gap yields a confident reply with NO file (silent fail); verb-gate new ambiguous cues, never add to the bare-noun gate.
- [Ora upload re-hydration & file-continuation](ora-upload-rehydration.md) — uploaded-file text is ephemeral session-scoped; client re-sends documentRefs each turn. "yes" routes to file gen ONLY when last assistant turn explicitly offered + named a format.
- [Ora sidebar standalone conversations](ora-sidebar-standalone-conversations.md) — Ora "New conversation" chats default to projectId=null; sidebar must always show a standalone/"Recent" section or they look deleted despite being persisted.
- [Vite manualChunks TDZ crash](vite-manualchunks-tdz.md) — hand-splitting interdependent vendor libs (recharts/d3, remark/rehype, xyflow) into manual chunks causes "Cannot access X before initialization" runtime crash before React mounts; rely on Rollup default chunking + lazy() route splitting.
- [Post-merge lockfile drift + husky git block](post-merge-lockfile-husky.md) — ERR_PNPM_OUTDATED_LOCKFILE after a merge breaks ALL workflows; fix with `pnpm install --lockfile-only` then a normal install (husky git block is post-install, node_modules still OK); Expo "failed" port detection is a false alarm.
- [Ora TTS needs direct OpenAI key](ora-tts-direct-key.md) — AI-integrations proxy rejects POST /audio/speech (INVALID_ENDPOINT→502); Talk to Ora TTS must use a direct OPENAI_API_KEY client; Whisper transcribe still works via proxy.
- [Ora auto-speak TTS](ora-auto-speak-tts.md) — "Voice responses on" toggle must auto-speak replies via server speakTextForce; merged effect gated by voiceConvActive?!muted:isTtsEnabled + armed dedup ref (seed-on-first-load, re-arm on empty) so loaded history never replays; keep ora-panel & ora-bubble in sync.
- [localStorage cache user scoping](localstorage-cache-user-scoping.md) — per-user localStorage caches must be keyed by userId + treated as cache not source-of-truth (clear on empty server) or transcripts leak across users on shared browsers.
- [Ora surface-column isolation](ora-surface-isolation.md) — a surface/tenant column on ora_conversations must be filtered on EVERY single-row CRUD endpoint (get/patch/put-messages/delete), not just the list query, or exposed IDs let the other surface read/mutate rows.
- [Drizzle explicit select drops new columns](drizzle-explicit-select-new-columns.md) — adding a display column needs every explicit .select({...}) projection updated (esp. list endpoints), not just schema+insert, or it renders blank after refetch.
- [Ora Projects active-project scope](ora-project-scope-tristate.md) — /ora/projects/:id route is source of truth; newConversation(projectId?) tri-state undefined/null/number must never collapse undefined→null or project chats save as standalone.
- [Clerk-expo web preview script error](clerk-expo-web-preview.md) — @clerk/expo throws loadClerkJsScript errors in the web preview only; native iOS/Android uses native token flow and is unaffected. Verify via typecheck + clean Metro bundle, don't chase the web error.
- [Stripe connector dev/prod price-id scoping](stripe-connector-env-scoping.md) — Replit Stripe connector has separate dev(test)/prod(live) connections; STRIPE_*_PRICE_ID must be env-scoped per environment, never shared (test IDs 400 in live mode).
- [Browser favicon SVG precedence](favicon-svg-precedence.md) — browsers prefer the `image/svg+xml` favicon over .ico/.png; a wrong SVG favicon overrides correct PNGs. Fixing PNG/ICO alone won't fix the tab icon — remove/replace the SVG too.
- [Ora web-search media](ora-web-search-media.md) — images/videos are model self-reported via a trailing ora-media JSON block (no provenance); every media+citation URL must pass isSafeHttpUrl's isPrivateOrLocalHost SSRF guard (dup'd backend+frontend); DNS-rebinding residual risk needs a media proxy. ALSO: chat.ts search branch is a separate path from the conversational branch — per-user memory/profile must be injected into BOTH or it silently fails for one.
- [pull-from-github merge conflict](pull-from-github-merge-conflict.md) — auto-run pull-from-github can leave an in-progress merge + conflicted pnpm-lock.yaml (dual @types/react TS2322 wall); main agent can't fix it (git writes blocked, pnpm install trips git guard) — resolve via a background task.
- [ORAX surface isolation + repo add](orax-surface.md) — ORAX must only call /api/orax/*; repo add is metadata_only (no GitHub OAuth needed); live testClerkAuth sign-in is blocked (redirects to /sign-in), verify via unit+route checks.
- [Ora image-intent routing is pattern-only](ora-image-intent-routing.md) — Ora "can't produce the image this turn" = ORA_IMAGE_PATTERNS missed (conversational fallback); router is sole image-intent decider, provider/config usually fine. Broaden patterns, don't touch provider.
- [Prerender SEO fallback FOUC](prerender-fouc.md) — public landing flashes unstyled HTML on refresh because prerender injects classless semantic HTML into #root; hide with inline [data-prerender-fallback]{display:none} (kept in raw HTML for bots).
- [Ora search integration tests TDZ](ora-search-test-tdz.md) — search-*.test.ts use `const createMock=vi.fn()` inside a hoisted vi.mock("openai") → TDZ "no tests" when loaded in isolation; fix the one you touch with vi.hoisted.
- [HMR "rendered more hooks" false positive](hmr-added-hook-false-error.md) — adding a hook to a live-edited file throws "Rendered more hooks"/"Should have a queue"; it's a stale Fast Refresh artifact that clears on full reload, not a conditional-hooks bug.
- [R2 image display private-endpoint trap](r2-image-display-private-endpoint.md) — with no CF_R2_PUBLIC_URL the stored image fileUrl is a private S3 endpoint that <img> can't load; serve bytes via auth /images/:id/file + persist data URLs, never trust raw fileUrl.
- [ORAX wiring test is a source-string snapshot](orax-wiring-literal-test.md) — orax-wiring.test.ts toContain()s literal orax.tsx fragments; refactoring a matched call site (e.g. selectedTask.id→targetTaskId) breaks it though source is fine; only Linux/Replit vitest catches it.
- [Radix overlay testing in jsdom](radix-jsdom-overlay-testing.md) — mustaflow test-setup.ts stubs hasPointerCapture/scrollIntoView so Radix overlays open; overlay content portals to document.body, query there not the render container.
- [OraMessageActions export UI](ora-message-actions-export-ui.md) — labeled downloads live in the mobile Radix Popover (text-labeled, no aria-label) + desktop OraExportMenu (button title="Export / Generate"); query by text, not aria-label.
- [Ora saved-memory embeddings & recall](ora-memory-embeddings.md) — Ora memory save path inserts without embeddings (not writeKnowledge); recall ranks cosine→TF-IDF fallback→recency + lazy backfill-on-read.
- [Ora memory surfaces & consolidation](ora-memory-surfaces.md) — /api/ora/memories (origin=ora) vs /api/knowledge (excludes ora); supersede-based conflict consolidation is conservative (≥2 shared tokens + 0.6 overlap) and reversible via superseded_by.
- [Ora Memory Center read path](ora-memory-center-read-path.md) — Memory Center must list/edit/delete via /api/ora/memories; /api/knowledge excludes origin="ora" so it silently shows "No saved memories yet".
- [TS 5.9 RegExpMatchArray filter-callback never](ts59-regexp-match-array-filter.md) — `str.match(/re/g) ?? []` yields `RegExpMatchArray | never[]`; TS 5.9 narrows filter-callback params to `never`. Fix: explicit `: string[]` on the match result.
- [Ora model-router vi.mock must export isDeepSeekAvailable + MODEL_DEFAULTS](ora-model-router-mock.md) — model-router.ts imports both as module-level values; mock factory missing them causes undefined() → Express 5 500 before any test asserts; mock every value exported at module scope.
- [Ora plan accent theming](ora-plan-accent-theming.md) — accent threads via `--ora-accent-hsl` CSS var on OraPanel root (not per-element); DynamicAtom needs accentColor prop; OraBubble is anonymous-only so its core/wave branches are dead.
- [DB pool idle drops & deploy "database diff" error](db-pool-idle-drops.md) — managed PG drops idle conns ("Connection terminated unexpectedly"); shared pg.Pool needs keepAlive+timeouts+error handler; deploy diff check is platform-side, safe to Republish since app self-migrates on boot.
- [Ora streaming "not working" = TTFT](ora-streaming-ttft.md) — SSE streams fine (prove via curl -N); the gap is sequential pre-stream awaits. Parallelize context builders; never block first token on the 5s summary AI call (background it, await before done).
- [Ora streaming empty bubble + "Replying…"](ora-streaming-empty-bubble.md) — don't render the empty streaming placeholder row; gate indicator/status on the TRAILING streaming msg having trimmed content, not "any assistant has content" (multi-turn bug).
- [Gemini 3 thinking slows streaming first-token](gemini3-thinking-ttft.md) — Ora "empty during streaming" = Gemini 3 thinking adds ~4-5s silent time-to-first-token; not a proxy-buffer or frontend bug.
- [Ora streaming quota branch gap](ora-streaming-quota-branch-gap.md) — /chat conversational branch had a direct incrementMessageCount call bypassing chargeSession; grep ALL charging call sites when adding a new charge parameter.
- [Vitest mock path must match importer's resolved module](vitest-mock-path-import-source.md) — mock path is resolved from the TEST file location; ai-provider-config ≠ ai-providers even though adjacent; wrong mock = real API calls + flaky failures.
- [runCandidateChain mock must return full CandidateChainResult](vitest-runCandidateChain-mock-shape.md) — returning "" causes chain.result.choices[0] TypeError; always mock with full { result: { choices:[{message:{content}}] }, usedFallback, candidate } shape.
