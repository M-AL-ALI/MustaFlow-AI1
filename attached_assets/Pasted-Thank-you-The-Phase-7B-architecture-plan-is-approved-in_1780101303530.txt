Thank you. The Phase 7B architecture plan is approved in direction, but not yet approved for implementation.

Please revise the plan with these corrections before coding.

1. Split Phase 7B into two safe tracks

Track A — File Generation Studio:

* DOCX
* XLSX
* PPTX
* PDF/print
* CSV/JSON/MD/TXT

Track B — Reporting Intelligence:

* better executive reports
* stronger prompts
* ReportResult schema
* 10 reporting modes

Do not mix both too aggressively in the first implementation. Start with file generation from existing safe Ora outputs, then add reportMode AI prompting after export stability is proven.

2. Client-side generation is approved, but keep bundle safe

Lazy imports are required.

Confirm that:

* docx
* xlsx
* pptxgenjs

are never included in the initial landing-page bundle.

They should load only when the user clicks an export/generate button.

3. PDF strategy needs correction

Browser Print API is acceptable for Phase 7B, but do not call it “download PDF” unless the browser opens the print dialog.

Label it clearly as:

* “Print / Save as PDF”

Do not promise silent PDF download unless a real PDF library/server renderer is added later.

4. Raw Dataset export must be off by default

Do not include Raw Dataset in Excel by default.

Raw Dataset sheet should only be included if:

* user explicitly chooses it
* the data is already part of the uploaded dataset session
* it passes sanitization
* it is capped
* it does not expose hidden/internal fields

Default Excel workbook should include:

* Summary
* KPI Results
* Findings
* Trends
* Root Cause
* Action Plan
* Recommendations
* Risk Matrix

5. Add export source choices

User should be able to export:

* current assistant response
* dataset analysis report
* root-cause report
* whole conversation
* selected message/report
* generated ReportResult

Do not force every export to use the whole conversation.

6. Add clear UI design

Use a dropdown or menu:

“Export / Generate”

Options:

* Word Report
* Excel Workbook
* PowerPoint Deck
* Print / Save as PDF
* Markdown
* JSON
* CSV Action Plan

Avoid cluttering the Ora header with too many icons.

7. Server-side AI reporting needs stricter rate limiting

If reportMode adds a new AI call or changes prompt complexity, add clear limits:

* max report generation requests per session
* max report generation requests per IP/hour
* max token output
* graceful error if model fails

If reportMode uses the existing chat endpoint, make sure it does not bypass existing Ora rate limits.

8. OpenAPI change should be questioned

If public Ora currently uses raw fetch and not generated Orval hooks, only add OpenAPI changes if the route is actually represented in the spec.

Do not add OpenAPI changes unnecessarily.

9. ReportResult schema needs sanitization and size limits

Add limits:

* max title length
* max executive summary length
* max number of findings
* max number of recommendations
* max action plan rows
* max PPTX slides
* max XLSX rows
* max JSON export size

10. Add file-generation safety tests

Tests must prove generated files do not include:

* fileRef
* imageRef
* datasetRef
* base64
* session token
* handoff token
* internal IDs
* secret/key/token fields
* hidden metadata
* Builder/project data

11. Add manual file-open validation

Required proof after implementation:

* DOCX opens in Word/Google Docs
* XLSX opens in Excel/Google Sheets
* PPTX opens in PowerPoint/Google Slides
* Print/Save as PDF works from browser
* Markdown opens correctly
* JSON is valid
* CSV opens correctly

12. Start with Sub-phase 7B-1 only

Approved next implementation plan should be limited to:

Sub-phase 7B-1:

* package install
* lazy import setup
* file-generation sanitizer
* DOCX export
* XLSX export
* integration into existing Ora export menu
* no reportMode AI changes yet
* no PPTX yet
* no PDF yet
* no ReportResult endpoint yet

After 7B-1 is validated, then continue with:

* 7B-2 PPTX
* 7B-3 Print / Save as PDF
* 7B-4 Reporting Intelligence / reportMode

13. Do not start implementation yet

Please return a corrected 7B-1 implementation plan only.

Include:

* files to create/modify
* exact packages
* lazy import proof
* sanitizer plan
* DOCX plan
* XLSX plan
* UI plan
* privacy plan
* tests
* rollback plan

After I approve the corrected 7B-1 plan, then implementation can begin.