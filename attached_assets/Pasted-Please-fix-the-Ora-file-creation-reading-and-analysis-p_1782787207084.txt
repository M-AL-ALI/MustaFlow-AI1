Please fix the Ora file creation, reading, and analysis parity gaps across website and mobile. Keep the existing strong backend validation/parsing, but address these specific issues: 1. Make /api/public-ai/export-file truly AI-free. It currently imports file-builder.ts, which loads AI provider env at module load, so deterministic export can fail without AI env even though the route is supposed to be no-AI/no-charge. Split pure builders like buildDocx, buildXlsx, buildPptx, and buildPdf into an AI-free module, or lazy-load AI only inside prompt-based generation. Export tests must pass without AI provider env. 2. On mobile, add all file-related public AI routes to pathRequiresAuth() so signed-in users fail closed instead of silently downgrading to anonymous during Clerk token races. Include /api/public-ai/upload, /api/public-ai/file-analysis, /api/public-ai/dataset-analysis, /api/public-ai/image-analysis, and /api/public-ai/export-file. Anonymous users should still work when truly signed out. 3. Add mobile UI parity for website “Generate file”: mobile should expose an explicit composer/sheet/menu for CSV, XLSX, DOCX, PDF, and PPTX generation instead of relying only on natural-language requests or exporting an existing answer. Wire it to the same backend behavior and keep generated files downloadable/shareable. 4. Improve durability/sync for uploaded file references. Today uploaded document refs are in-memory/TTL only, so reloads, device changes, or later conversation restores can lose file context. For signed-in users, persist a durable assetId or file reference mapping so Ora can rehydrate uploaded files for follow-up questions across website and mobile. 5. Improve generated-file history durability. Since fileData is stripped before persistence, generated file cards can lose download capability after reload. Store a durable assetId or download URL with generated file metadata, and make website/mobile cards open the Library asset or download endpoint when inline bytes are missing. 6. Fix the failing dataset fallback test in phase3.test.ts: the test expecting invalid primary JSON to fall back successfully currently returns 502. Diagnose whether candidate ordering, provider mock setup, or fallback handling changed, and restore confidence that dataset analysis gracefully falls back. Verification required: api-server, mustaflow, and ora-mobile typechecks clean; mobile parity/auth/safe-url tests pass; backend export-file/export-content tests pass without AI env; dataset fallback test passes; and add/update tests proving mobile file routes require auth for signed-in users and explicit mobile file generation is wired.