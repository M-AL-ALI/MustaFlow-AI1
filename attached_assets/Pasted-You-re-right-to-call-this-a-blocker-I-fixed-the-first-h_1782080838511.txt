You’re right to call this a blocker. I fixed the first hard failure path and committed it locally.

Commit: 1b6f4ba6519025a4e64e3e13bc624b8b31b930d6
Branch: codex/ora-mobile-branding
Repo path: C:\Users\mus_1\Documents\Mustaflow AI\.compare-github\MustaFlow-AI1

What I fixed:

* TestFlight backend wiring: EXPO_PUBLIC_DOMAIN=mustaflow.com is now pinned for testflight and production EAS builds.
* Native API client no longer falls back to an empty URL. It now resolves Ora API calls to https://mustaflow.com if the env is missing.
* Restored Expo slug to mustaflow-ai, which matches the registered EAS project.
* Added a visible config screen if the Clerk publishable key is missing instead of silently breaking auth.
* Added Settings → Diagnostics showing API URL, streaming flag, Clerk key presence, Expo slug, and a “Test Ora backend” button.
* Fixed Library asset URLs to use the same centralized Ora API URL resolver.
* Started website-shell parity on the main chat screen: Ora logo header, tier pill, temporary pill, website-style greeting, same example chips, and a unified rounded composer like the website Ora panel.

I did not touch the website Ora model router, provider routing, or AI Builder code. Mobile still calls the same Ora backend endpoints: /api/public-ai/chat, /api/public-ai/chat/stream, /api/ora/*, /api/orax/*. No mobile-only model routing was added.

Verification:

* Mobile typecheck: passed
* Prettier: passed
* ESLint on touched mobile files: passed
* Mobile unit tests: 12/12 passed
* Ora code-only isolation test: 11/11 passed
* DB-backed Builder memory isolation test could not run here because DATABASE_URL is not set

Important: this fixes the likely reason “hi” does not reply, but it still needs a new TestFlight build from this commit. The current TestFlight should be considered failed/stale.

Next required step: push/merge this commit, rebuild TestFlight, open Settings → Diagnostics, tap “Test Ora backend,” then send “hi.” After that, we should do a strict side-by-side website/mobile parity pass for every remaining screen.