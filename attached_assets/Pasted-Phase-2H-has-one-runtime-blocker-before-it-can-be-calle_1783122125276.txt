Phase 2H has one runtime blocker before it can be called complete.

Backend queues run_project_thread, but Orax Desktop relay does not handle it.

Evidence:

artifacts/api-server/src/routes/orax-projects.ts queues:
type: "run_project_thread"

But artifacts/orax-desktop/src/main/relay-client.ts executeAction only handles:
- ping_desktop
- get_desktop_status
- list_local_projects
- run_safe_command

Then it falls through to:
Unsupported action type

Required fix:

1. Add run_project_thread handling inside relay-client.ts.

When action.type === "run_project_thread":
- Read payload:
  - projectId
  - threadId
  - executionSourceId
  - sourceLocalPath
  - userMessage
- Validate payload fields.
- Verify sourceLocalPath exists.
- Verify `.orax/project.json` exists or create it only if safe.
- If `.orax/project.json` exists with a different projectId, fail the action.
- Verify executionSourceId matches if present.
- Post action event "running".
- For now, do not run arbitrary coding commands yet.
- Return a completed result like:
  {
    projectId,
    threadId,
    executionSourceId,
    localPathVerified: true,
    message: "Project thread context verified on desktop."
  }

2. Do not rely only on renderer IPC.

Current `project:runProjectThread` IPC is not enough because backend actions are delivered through relay-client polling. The relay client itself must handle the action.

3. Add wiring tests.

Add assertions that:
- relay-client.ts contains "run_project_thread"
- relay-client.ts reads sourceLocalPath
- relay-client.ts checks `.orax/project.json`
- relay-client.ts posts "running" before completing
- relay-client.ts does not fall through to Unsupported action type for run_project_thread

4. Re-run verification:

pnpm run typecheck:libs
pnpm --filter @workspace/api-server run typecheck
pnpm --filter @workspace/orax-desktop run typecheck
pnpm --filter @workspace/orax-desktop run build
pnpm --filter @workspace/mustaflow run typecheck
pnpm --filter @workspace/mustaflow run lint
pnpm --filter @workspace/ora-mobile run typecheck
pnpm --filter @workspace/mustaflow run test -- src/lib/__tests__/orax-wiring.test.ts

Do not move to Phase 2I until this is fixed.