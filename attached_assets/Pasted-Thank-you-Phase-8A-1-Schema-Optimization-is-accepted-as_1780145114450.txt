Thank you. Phase 8A.1 Schema Optimization is accepted as complete.

Accepted commit:

* 7a419e70abf989d9e1a94ca6975fb727aec1afef

Accepted scope:

* vault_entries.tags converted from TEXT to TEXT[]
* vault_versions.tags converted from TEXT to TEXT[]
* existing comma-delimited tags migrated safely
* tags now default to empty array
* tags are NOT NULL
* OpenAPI updated
* generated API clients updated
* frontend Vault page updated for array-based tags
* Vault route updated for array-based tags and full-text search

Accepted database improvements:

* vault_entries_tags_idx created with GIN on tags
* vault_entries_search_idx created with GIN functional index on to_tsvector(...)
* vault_entries_updated_idx created
* vault_entries_dept_idx created
* vault_entries_archived_idx created
* no broken/generated search_vector column remains

Accepted PostgreSQL design decision:

* A generated tsvector column was attempted but rejected due to PostgreSQL immutable-expression requirements.
* Final design uses a functional GIN index instead.
* This is accepted because the route query mirrors the index expression and preserves the intended planner benefit without requiring a generated column.

Accepted search behavior:

* keyword search now uses to_tsvector(...) @@ plainto_tsquery(...)
* title search works
* summary search works
* tag filtering works through native TEXT[] containment
* category filter still works
* department filter still works
* status filter still works
* archived toggle still works
* pagination still works

Accepted API contract:

* VaultEntry.tags is now string[]
* VaultVersion.tags is now string[]
* VaultEntryInput.tags remains string[]
* no comma-delimited tag behavior remains in the generated API contract

Accepted frontend behavior:

* Vault page handles tags as arrays
* edit flow handles tags as arrays
* detail viewer renders tags correctly
* tag pills render directly from arrays
* dataset card Save to Vault remains functional
* existing vault entries render correctly after migration

Accepted backward compatibility proof:

* old tags such as safety,quality,wbi migrated to ["safety", "quality", "wbi"]
* multi-word tags were preserved correctly
* empty tags became []
* no entries lost tags
* no version rows lost tags

Accepted security proof:

Phase 8A.1 did not weaken:

* sensitive content blocking
* sk-proj / sk-ant key blocking
* Bearer token blocking
* JWT blocking
* database connection string blocking
* base64 blocking
* ownership checks
* authentication checks

Accepted regression proof:

* Phase 8A CRUD still works
* create vault entry works
* edit/versioning works
* archive/restore works
* search/filter works
* save from dataset card works
* public Ora remains isolated
* reporting/export stack remains untouched
* Builder Mode remains unaffected
* Developer Mode remains unaffected
* Plan Mode remains unaffected

Accepted quality proof:

* migration run passed
* typecheck passed
* lint passed
* format passed
* codegen drift passed
* quality gate passed
* API server running
* frontend running

Phase 8A.1 is now closed and part of the stable Knowledge Vault baseline.

Current stable vault baseline:

* Phase 8A — Knowledge Vault Foundation
* Phase 8A.1 — Tags array + full-text search/index foundation

This is now a stronger foundation for Phase 8B.

Do not start Phase 8B, embeddings, semantic search, RAG, AI memory, auto-learning, Web Search, Image Generation, Advanced Charts, or unrelated roadmap work until I explicitly approve.