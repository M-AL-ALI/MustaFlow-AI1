Accepted as Ora Production Safety Wave 1B Core.

Completed:

* global daily Ora unit cap
* anonymous per-IP daily unit cap
* feature unit costs
* midnight UTC reset
* structured daily spend-cap 429 response
* cap-hit structured logging
* E2E/test bypass
* spend-cap wiring for:
    * /public-ai/chat
    * /public-ai/chat/stream
    * /public-ai/file-analysis
    * /public-ai/dataset-analysis
    * /public-ai/generate-file
    * /public-ai/image-analysis
* 27 new spend-cap tests
* final quality gate passed: 1408 passed / 2 skipped
* changes pushed to GitHub

Important:
Do not mark the full Production Safety Wave 1B complete yet until the remaining original requirements are confirmed.

Still needed:

1. Per-user daily cap for authenticated users

* Current summary confirms global cap and anonymous per-IP cap.
* It does not clearly confirm a per-user authenticated daily cap.
* Add a per-user daily unit cap keyed by authenticated user ID.
* It should apply across all Ora features for that user.
* It should return the same structured daily_spend_cap response when reached.

2. Cover all costly Ora features
    Confirm whether these routes/features are also protected by daily unit caps:

* web search
* TTS / voice
* transcribe / STT
* image generation, if separate from image analysis
* any other public Ora route that can call an AI provider or paid service

3. Tests still needed
    Add or confirm tests for:

* authenticated per-user daily cap reached
* authenticated user cap does not affect another authenticated user
* anonymous IP cap does not affect authenticated user cap
* web search daily unit tracking
* TTS/voice daily unit tracking
* transcribe daily unit tracking, if public
* image generation daily unit tracking, if public
* all cap responses contain no AI Builder / Builder / handoff language

Acceptance:
Production Safety Wave 1B is fully complete only when:

* global daily cap works
* anonymous per-IP daily cap works
* authenticated per-user daily cap works
* every costly public Ora route is covered
* all tests pass
* quality gate passes
* changes are pushed to GitHub

Next step:
Close Wave 1B remaining gaps: authenticated per-user daily cap + missing feature-route coverage.