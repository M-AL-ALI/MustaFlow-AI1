Thank you. The combined validation report is mostly accepted, but I need one privacy correction before I fully accept the Arabic voice/transcription work.

Accepted so far:

* upload button visibility is confirmed
* drag-and-drop upload is implemented
* paste-to-upload is implemented
* normal text paste remains unaffected
* uploads still use POST /api/public-ai/upload
* no new upload route was added
* existing validation/limits remain in place
* Arabic browser SpeechRecognition uses ar-SA
* Whisper path now sends lang=ar
* backend transcribe route validates lang
* speechToText(..., "ar") passes the Arabic language hint to gpt-4o-mini-transcribe
* Arabic/RTL display uses dir="auto" on composer, message bubbles, and voice live areas
* Arabic transcript lands in composer for review/edit
* quality checks pass
* rollback plan is clear

Required correction before full acceptance:

The report says transcribe.ts logs:

{ component: "ora-transcribe", bytes: total, format, language: language ?? "auto", sessionId: session.sessionId }

Please do not log the raw sessionId.

This should be changed to one of these safe options:

Option A — remove sessionId from logs completely

or

Option B — log only a one-way hash:

sessionIdHash: sha256(session.sessionId).slice(0, 12)

Do not log:

* raw sessionId
* raw token
* transcript text
* audio content
* user message text
* file content
* image/base64 content

Safe logs may contain:

* component
* bytes
* format
* language code, such as ar
* latency
* error category
* hashed session ID only, if needed

After this privacy fix, please provide:

1. Final commit hash
    Confirm the commit hash that includes the sessionId logging fix.
2. Updated log proof
    Show that transcribe.ts no longer logs raw sessionId.
3. Quality checks
    Confirm again:

* typecheck
* lint
* format
* frontend workflow
* backend workflow

4. Arabic live/staging note
    Since a real microphone test was not possible in the environment, please add this note to the final record:

“Arabic voice transcription should still be manually verified in staging/production with a real microphone and Arabic speech before production launch.”

Manual Arabic staging checklist:

* select Arabic language
* test normal mic/dictation
* confirm transcript appears in Arabic script
* confirm transcript lands in composer first
* confirm user can edit before sending
* send Arabic message
* confirm Ora replies in Arabic
* test Talk with Ora / Voice Conversation Mode
* confirm Arabic transcript appears in the thread
* confirm RTL display looks correct
* confirm no transcript/audio appears in logs

After the raw sessionId logging is removed or hashed, I will accept the combined upload UX + Arabic voice fix as complete.

Do not start Phase 7, Phase 8, Phase 9, thread scrolling, image generation, Knowledge Vault, or unrelated roadmap work under this task.