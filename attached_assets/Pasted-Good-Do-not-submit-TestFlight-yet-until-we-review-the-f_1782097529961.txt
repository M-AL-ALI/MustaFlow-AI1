Good. Do not submit TestFlight yet until we review the final parity proof.

The Deep Thinking entitlement fix is important and correct:
- Free users must not be able to use Deep Thinking.
- Core Pack and Deep Wave should be allowed.
- Mobile must follow the same backend/session tier logic as website.

However, I do not want the statement “mobile is ready for TestFlight” until we verify the full parity proof.

Please provide the final evidence package first:

1. Final parity audit table
   Include every feature, not just summaries:
   Website Ora feature | Website behavior | Mobile behavior | Difference | Status | Evidence file/line or screenshot

2. Light/dark visual proof
   Provide side-by-side screenshots:
   - website Ora light vs mobile light
   - website Ora dark vs mobile dark
   - empty chat
   - active chat with Ora reply
   - plus/tools menu
   - reply language selector
   - plan badge for available plan
   - settings theme picker
   - diagnostics screen

3. Runtime proof before TestFlight
   Even before a new TestFlight submission, verify in dev/simulator if possible:
   - Diagnostics transport passes
   - /api/public-ai/session passes
   - /api/public-ai/chat temporary “hi” returns a reply
   - main chat “hi” returns an Ora reply
   - stream either works or safely falls back

4. Plan entitlement proof
   Show how mobile gates:
   - Free
   - Core Pack
   - Deep Wave
   - anonymous/signed-out state if applicable

   Confirm:
   - Deep Thinking disabled for Free
   - Deep Thinking allowed for Core/Wave
   - labels and plan colors match website
   - no mobile-only plan logic

5. Native adaptation clarification
   Do not mark anonymous access mismatch as “acceptable” unless we explicitly approve it.
   Website allows unsigned users, so if mobile requires sign-in, that is still a parity difference.
   Please document:
   - why mobile cannot support anonymous
   - what behavior signed-out users see
   - whether anonymous chat is technically possible through /api/public-ai/session
   - whether we want to keep or fix this before release

6. Visual detail gap
   The note about tier accent color in active UI states is not automatically low priority.
   If website threads plan accent colors through active controls, mobile should mirror it unless there is a reason not to.
   Please list exactly which mobile controls still use fixed purple instead of plan accent:
   - mode selected state
   - language selected state
   - active chips
   - plan badge
   - buttons/links if applicable

7. Ora isolation proof
   Confirm again:
   - no AI Builder UI
   - no Builder CTA
   - no Builder handoff
   - no /api/builder/*
   - no /public-ai/handoff/create
   - no mobile-side model/provider routing

Do not submit a new TestFlight build until this evidence package is complete and we approve the remaining differences.