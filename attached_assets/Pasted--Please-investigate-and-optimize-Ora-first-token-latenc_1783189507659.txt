“Please investigate and optimize Ora first-token latency across all surfaces: authenticated website Ora, public landing-page Ask Ora bubble, and Ora Mobile. Do not change the existing word-by-word streaming cadence/rendering on website or mobile. The issue is not the visual streaming once tokens start; the issue is the delay before the first visible word appears. Target: Instant mode simple prompts should show the first token in ≤1 second when possible, and absolutely should not sit at 3–5 seconds. If the provider/network makes <1s impossible for a specific request, log exactly which bucket caused it.
Scope:
Website authenticated Ora
Route: /ora
Uses use-ora-chat.ts
Must keep existing /api/public-ai/chat/stream behavior and word-by-word pacing untouched.

Website public landing Ask Ora bubble
Route: homepage / landing page
Also uses useOraChat()
Must be included in the latency fix. Do not only optimize signed-in /ora.

Ora Mobile
Uses streamChatNative()
Must use the same backend /api/public-ai/chat/stream
Must not fall back to /api/public-ai/chat unless streaming truly fails.
Keep the existing word-by-word UI queue unchanged.

What to measure first:
Add or verify end-to-end latency diagnostics with a shared request ID across client and server. For every Instant chat request, log:
Client-side:
sendPressedAt
requestStartedAt
streamHeadersAt
firstTokenAt
doneAt
surface: web-authenticated, web-landing, or mobile
mode: instant or deep
transport: sse-fetch, xhr-sse, or fallback
whether fallback to /chat happened
first-token latency from tap/send to first visible token
Server-side:
requestReceivedAt
sessionMs
authMs
spendCapMs
quotaMs
routingMs
classifierMs
contextMs
modelStartMs
ttftMs
streamMs
totalMs
selected model/provider tier
whether fast-lane was used
whether classifier was skipped/timed out
whether context was skipped, cached, or timed out
Do not guess. Use production logs from all three surfaces and identify the actual slow bucket.
Optimization requirements:
Instant fast path must be enforced consistently
For simple Instant prompts with no attachments, no image request, no file generation, no web-search requirement, and no Deep mode:Skip expensive classifier waits.
Use the fastest approved Ora model route.
Keep the first-token path minimal.
Use tight context budgets.

This must apply equally to:authenticated website Ora
landing-page Ask Ora bubble
Ora Mobile


Do not let classifier delay block first token
If classifier is still used, it must run in parallel or have a very short timeout.
For obvious simple Instant prompts, bypass it entirely.
If classifier takes longer than the budget, use safe fast-lane fallback instead of waiting.

Do not let memory/context delay block first token
For Instant simple prompts, memory/profile/history context must be cached, preloaded, parallelized, or capped tightly.
If memory is not ready inside the budget, start the response and do not wait 3–5 seconds.
Deep mode can keep richer context; Instant mode must prioritize speed.

Session and auth must be pre-warmed
Website landing Ask Ora and authenticated Ora should create/preload the Ora session before the user sends the first message if possible.
Mobile should preload session/auth readiness after app launch/sign-in.
The first user send should not spend 500ms–1500ms just minting session/auth state.

Mobile must not silently fall back
If mobile falls back from /chat/stream to /chat, diagnostics must say why.
Fix any fallback cause that is still happening on TestFlight.
But do not alter word-by-word streaming behavior unless the diagnostic proves UI batching is still the problem.

Landing-page Ask Ora must be included
The public Ask Ora bubble is a user-facing first impression.
It should get the same fast-lane and prewarm behavior as signed-in Ora.
If anonymous quota/session handling adds latency, optimize or prewarm it.

Provider/model routing
For Instant simple prompts, route to the fastest reliable model.
If the current provider has 2–4 second TTFT, it is not acceptable for Instant.
Use a faster model for Instant and reserve slower/deeper providers for Deep Thinking, research, file analysis, image/video, or complex reasoning.

Do not change:
Do not change website word-by-word streaming cadence.
Do not change mobile word-by-word streaming cadence.
Do not redesign Ora UI.
Do not reduce Deep mode quality.
Do not break file/image/search/research routes.
Do not touch Talk to Ora voice unless logs prove it is part of this text-chat latency issue.
Acceptance criteria:
For a simple Instant prompt like “hi”, “what is 2+2”, or “summarize this idea in one sentence”:Website authenticated Ora first visible token target: ≤1s.
Landing Ask Ora bubble first visible token target: ≤1s.
Ora Mobile first visible token target: ≤1s if network is healthy.
If any surface exceeds 2s, logs must clearly show the slow bucket.

No regression to word-by-word streaming.
No silent mobile fallback to /chat.
Production logs must show which surface each request came from.
Add tests or source-level guards proving the Instant fast path applies to all three surfaces.
After deploy/TestFlight, run manual QA on all three: website /ora, landing Ask Ora bubble, and Ora Mobile.
Most likely fix direction:
Implement a shared ‘Instant Fast Reply Path’ in the backend /api/public-ai/chat/stream route, not separate hacks per client. Then make sure all three clients actually use that same stream route and send enough metadata to identify the surface. The fastest gains should come from skipping classifier waits, prewarming session/auth, caching or tightly budgeting memory context, and routing simple Instant prompts to the fastest model. Website and mobile should differ only in transport/rendering, not backend latency behavior.”