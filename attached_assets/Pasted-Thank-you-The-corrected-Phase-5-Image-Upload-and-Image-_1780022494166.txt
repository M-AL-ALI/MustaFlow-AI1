Thank you. The corrected Phase 5 Image Upload and Image Analysis plan is approved with the final corrections below.

You may proceed with Phase 5 implementation only after applying these corrections.

Approved Phase 5 scope:

* image upload for Ora public assistant
* image analysis only
* PNG support
* JPG/JPEG support
* WEBP support
* image thumbnail preview
* screenshot review
* UI/UX screenshot feedback
* logo/brand review
* diagram explanation
* chart/image explanation
* visible text reading when clear enough
* troubleshooting from screenshots/photos with safe limitations
* no permanent image storage
* no Builder/project access
* no credit/billing behavior

Not approved:

* image editing
* image generation
* background removal
* style transfer
* GIF support
* SVG support
* HEIC/HEIF support
* web search
* Builder handoff
* Knowledge Vault implementation
* permanent image storage
* project creation
* billing/credit changes
* training/learning from uploaded images

Final corrections before coding:

1. Verify sharp availability before relying on it
    The plan says sharp is already available. Please verify it is actually installed and usable in the api-server workspace.

If sharp is not installed, provide the exact package addition and confirm it works in the Replit/Linux environment before continuing.

2. Keep image limit wording clear
    The approved public image limit is:

* max 2 image uploads per session
* max 2 image-analysis calls per session

So the 3rd successful image upload should return safe 429.

Please update test wording from “Image count limit (3rd upload in session)” to:

“3rd image upload in same session returns safe 429 because the limit is 2.”

3. Do not use Builder credits
    Confirm again:

* no credit preflight
* no credit deduction
* no user_credits import
* no billing import
* no Power/Pro pricing logic
* public-AI rate/session limits only

4. Confirm model vision support before implementation is marked complete
    Before declaring Phase 5 complete, run a real live smoke test:

* valid image sent to selected vision model
* model returns non-empty accurate description
* no raw model error exposed
* selected model ID reported
* if gpt-5.4 vision fails, return graceful message and do not fall back to an untested provider silently

5. Image store memory cap
    The plan says max 200 entries × 4 MB = approximately 800 MB worst case. That is high for a public anonymous feature.

Please lower or justify the global cap.

Recommended:

* max global image entries: 50 initially, not 200
* max global bytes cap if practical, for example 200 MB
* evict oldest safely if exceeded
* return safe capacity message when needed

6. EXIF and metadata
    Confirmed requirement:

* strip EXIF/metadata before storing/sending
* do not log EXIF
* do not expose metadata to model unless intentionally needed, which Phase 5 does not need

7. Frontend object URLs
    When using URL.createObjectURL(file) for preview, ensure:

* revokeObjectURL is called when chip is removed
* revokeObjectURL is called when upload is cleared after successful analysis
* revokeObjectURL is called on component unmount
* avoid memory leaks

8. Image analysis count should increment only after successful analysis
    Rejected/failed analysis attempts should not consume the session’s 2-analysis limit unless the model was actually called.

If validation fails, expired ref, cross-session ref, or model not called, do not increment imageAnalysisCount.

9. Image upload count should increment only after successful accepted upload
    Rejected files must not consume the 2-image session limit.
10. Safe user-facing errors
    All error messages must be safe and simple:

* oversized image
* unsupported image
* invalid image bytes
* image expired
* image analysis capacity
* vision model unavailable
* unsafe/high-risk image content if detected

Do not expose:

* stack traces
* sharp errors
* model raw errors
* storage internals
* server paths
* base64 content

11. OCR wording
    Keep wording realistic:
    “I can read visible text when it is clear enough, but blurry or low-resolution images may be incomplete.”
12. Machine/equipment image safety
    For equipment/machine/electrical/chemical/pressure/food-safety images, Ora must:

* give high-level troubleshooting only
* identify possible visible concerns carefully
* recommend qualified professionals or proper safety procedures when needed
* avoid instructing dangerous repairs
* avoid bypassing safety systems
* avoid live electrical work instructions

13. UI states required
    Please implement and validate:

* image upload button through existing paperclip
* thumbnail preview chip
* uploading state
* attached state
* error state
* remove state
* oversized image error
* unsupported image error
* retry after analysis failure
* mobile UI proof
* Ora Panel proof
* Ora Bubble proof

14. Tests required
    Include all tests from your plan, plus:

* object URL cleanup test or source proof
* rejected image does not increment imageCount
* failed analysis before model call does not increment imageAnalysisCount
* model failure returns safe message
* gpt-5-nano/gpt-5-mini not used
* credit/billing imports not present
* sharp errors are hidden from visitors
* EXIF not logged

15. Final validation report required
    After implementation, provide full Phase 5 validation report with:

* affected files
* final commit hash
* final routes
* sharp availability proof
* model smoke-test proof
* PNG live upload + analysis
* JPG live upload + analysis
* WEBP live upload + analysis
* fake renamed image rejection
* oversized image rejection
* cross-session imageRef blocked
* expired imageRef safe response
* session image limit proof
* image analysis limit proof
* no credit/billing proof
* no DB/project/secret/Builder import proof
* no raw image/base64/EXIF/log leakage proof
* UI screenshots/proof
* mobile proof
* model used
* fallback/safe failure behavior
* typecheck/lint/format/quality gate status
* Phase 1/2/3/Voice-A regression proof
* rollback plan

Proceed with Phase 5 implementation only after applying these corrections. Do not start any other phase.