{
  "name": "@workspace/snapshot-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that serves MustaFlow published project snapshots from R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node -e \"console.error('Deployment is blocked: no live snapshot Worker exists and the checked configuration contains placeholders.'); process.exit(1)\"",
    "deploy:production": "node -e \"console.error('Production deployment is blocked: no live snapshot Worker exists and the checked configuration contains placeholders.'); process.exit(1)\"",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^3.114.17"
  }
}
