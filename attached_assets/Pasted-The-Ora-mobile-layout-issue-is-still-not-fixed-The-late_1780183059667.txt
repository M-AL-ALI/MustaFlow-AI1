The Ora mobile layout issue is still not fixed.

The latest screenshot confirms the assistant reply text is still rendering underneath the composer/input area. The text “In simple terms…” and the next assistant lines are hidden behind the input bar, upload hint, and message-count row.

This means the message thread is still not reserving enough space for the composer/footer, or the composer is still overlaying the feed instead of living as a true separate flex-shrink-0 layout section.

Please treat this as an active blocker and fix it properly.

Current visible problem:

* Assistant message text continues behind the chat composer.
* The composer/input bar is visually on top of the assistant reply.
* The upload hint/message count row overlaps the message text.
* The user cannot comfortably read the last response.
* The message thread bottom is not protected from the composer height.
* The previous fix did not fully solve the bottom spacing / scroll-container layout.

Likely remaining root cause:

The composer/footer area is either:

1. still overlaying the feed, or
2. the feed does not have enough bottom padding, or
3. the mobile drawer uses max-height instead of a real constrained height, or
4. the feed/composer are not structured as true sibling flex sections, or
5. the scroll container is not the correct element, so the last message scrolls underneath the composer.

Required final layout:

The Ora mobile drawer must be structured like this:

* outer drawer: fixed mobile-safe container with a real height
* header: flex-shrink-0
* message feed wrapper: flex-1 min-h-0
* message feed: overflow-y-auto
* composer/footer: flex-shrink-0
* no absolute/fixed composer overlaying the message feed
* no message text can render behind the composer
* feed must have bottom padding equal to composer/footer height if any overlay/sticky behavior remains

Preferred fix direction:

Use a true flex column layout:

<div className="fixed inset-x-0 bottom-0 flex max-h-[calc(100dvh-...)] flex-col">
  <Header className="flex-shrink-0" />
  <div className="relative flex-1 min-h-0">
    <div className="h-full overflow-y-auto pb-[composer-safe-space]">
      messages here
    </div>
  </div>
  <div className="flex-shrink-0 pb-[safe-area]">
    composer here
    upload hint / message count here
  </div>
</div>

Important:

* Do not use a composer that visually overlays the feed unless the feed has matching bottom padding.
* Do not rely only on h-full inside an unconstrained parent.
* Do not use absolute inset-0 in a way that collapses the parent.
* Do not let the upload hint/message count row sit on top of messages.
* The last assistant message must be fully readable above the composer.
* The user must be able to scroll until the last line of the last assistant message is above the input bar.

Please inspect and fix mainly:

* artifacts/mustaflow/src/components/ora-bubble.tsx

Also check shared or related layout in:

* artifacts/mustaflow/src/components/ora-panel.tsx
* artifacts/mustaflow/index.html

Required validation:

1. Use a long assistant response that is longer than the visible feed.
2. Scroll to the bottom.
3. Confirm the final line of the assistant response is fully visible above the composer.
4. Confirm the composer does not cover message text.
5. Confirm the upload hint and “messages left” line do not cover message text.
6. Confirm the thread scrolls normally.
7. Confirm the “Latest” button still works.
8. Confirm iPhone mobile viewport works.
9. Confirm Android mobile viewport works.
10. Confirm desktop layout still works.

Acceptance criteria:

* No text behind composer.
* No text behind upload hint.
* No overlap at the bottom of the thread.
* Last assistant message fully readable.
* Composer stays visible and usable.
* Feed scrolls correctly.
* Works on mobile iPhone and Android.
* Works on desktop.
* No Phase 8B, semantic search, RAG, AI memory, Knowledge Vault intelligence, Web Search, Image Generation, Advanced Charts, or unrelated roadmap work should be started.

Please provide the final commit hash, affected files, root cause, and validation proof after the fix.