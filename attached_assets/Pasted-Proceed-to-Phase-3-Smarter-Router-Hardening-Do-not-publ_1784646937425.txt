Proceed to Phase 3: Smarter Router Hardening.

Do not publish. Do not submit TestFlight.

Goal:
Make Ora’s routing more reliable across chat, files, search, images, uploaded-file edits, and mobile/website parity.

Requirements:
1. Audit the current router/orchestrator and all client send paths.
2. Add deterministic routing rules for obvious cases:
   - uploaded file edit must beat normal chat
   - uploaded file edit must beat image CTA unless the user explicitly asks for image generation
   - uploaded file edit must beat search unless the user explicitly asks for current/live info
   - current/live questions route to search
   - image creation/editing route to image tools
   - file generation routes to file tools
   - ZIP/code analysis routes to analysis, not file generation, unless user asks for report/export
3. Ensure both website and mobile send the same routing-critical fields:
   - documentRefs
   - conversationId
   - mode
   - timeZone
   - forceSearch
   - file/generation context
4. Add diagnostics for route/tool choice:
   - selected tool
   - reason
   - classifier skipped/used
   - search used
   - file edit inferred format
   - image/file/search conflict resolution
5. Add regression tests from past bugs:
   - Word edit returned image CTA
   - uploaded file edit returned text-only answer
   - mobile dropped documentRefs
   - live search retry forced search
   - “what is image generation?” stays chat
   - “I asked for image for World Cup 2026” routes image
   - logo with word “word” does not become Word doc
   - “just say the word” does not hijack file generation
   - sports/current info routes search
   - ZIP analysis remains analysis unless export requested
6. Update ORA_FEATURE_REGISTRY if needed.
7. Run fast gate after implementation.
8. Commit as a scoped Phase 3 commit.
9. Push to GitHub.
10. Run fast gate again on clean tree.
11. Report:
   - commit SHA
   - changed files
   - backend impact
   - website impact
   - mobile impact
   - tests added
   - fast gate result
   - known limitations

No publish/TestFlight until all selected phases are complete and the full release gate passes.