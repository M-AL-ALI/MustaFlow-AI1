Phase 2J is verified clean locally.

All checks passed:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/orax-desktop run typecheck
- pnpm --filter @workspace/orax-desktop run build
- pnpm --filter @workspace/mustaflow run typecheck
- pnpm --filter @workspace/mustaflow run lint
- pnpm --filter @workspace/ora-mobile run typecheck
- pnpm --filter @workspace/mustaflow run test -- src/lib/__tests__/orax-wiring.test.ts

Wiring tests: 152/152.

Before Phase 2K, please do one small hardening patch for Phase 2J:

Issue:
`project-file-reader.ts` already blocks symlink escapes before reading, so file contents are protected. However, `project-file-selector.ts` can still traverse symlinked directories during candidate selection. That could waste scan budget or expose candidate filenames from outside the project root, even though the reader would later refuse to read them.

Required fix:
Update `artifacts/orax-desktop/src/main/project-file-selector.ts` so candidate traversal does not enter symlinked directories.

Implementation requirements:
- In `walkForCandidates`, before recursing into any directory, check whether the entry is a symlink.
- If it is a symlink, skip it and optionally add a skipped/warning entry.
- If needed, use `fs.lstatSync()` or `Dirent.isSymbolicLink()`.
- Do not follow symlinks.
- Keep existing limits:
  - MAX_TRAVERSAL_DEPTH
  - MAX_CANDIDATE_FILES
  - MAX_SELECTED_FILES
- Keep blocked dirs:
  - node_modules
  - .git
  - dist
  - build
  - out
  - coverage
  - .next
  - .expo
  - .turbo
  - .cache
- Keep no shell usage:
  - no exec(
  - no spawn(
  - no shell: true
  - no process.cwd() fallback

Add wiring test assertions:
- `project-file-selector.ts` contains `isSymbolicLink` or `lstatSync`.
- `project-file-selector.ts` skips symlinked directories before recursion.
- selector still has no `exec(`, no `spawn(`, no `shell: true`, no `process.cwd()`.

Run:
pnpm --filter @workspace/orax-desktop run typecheck
pnpm --filter @workspace/mustaflow run test -- src/lib/__tests__/orax-wiring.test.ts
pnpm --filter @workspace/orax-desktop run build

Then push the fix.