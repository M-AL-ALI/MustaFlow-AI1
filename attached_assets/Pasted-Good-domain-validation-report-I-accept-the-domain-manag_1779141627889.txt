Good domain validation report. I accept the domain management foundation for internal testing.

Do NOT publish production yet.

The only major blocker is SSL automation. Domain save, DNS verification, custom routing, public snapshot isolation, and ownership protection look good.

Please start Phase 4B: SSL + Production Domain Readiness.

==================================================
PHASE 4B GOAL
==================================================

Make domain publishing production-ready.

Current status:
- Platform subdomains work as a foundation.
- Custom domains can be saved and DNS-verified.
- Custom domain routing serves frozen published snapshots.
- SSL status is only a status field.
- Automatic SSL certificate provisioning is not connected.

We need to either implement real SSL automation or clearly lock custom domains behind manual SSL setup until the SSL system is ready.

==================================================
TASK 1 — DECIDE SSL STRATEGY
==================================================

Research and recommend the best SSL strategy for MustaFlow AI.

Options:
1. Caddy automatic HTTPS
2. Let's Encrypt ACME worker
3. Cloudflare for SaaS
4. AWS ACM / CloudFront
5. Hosting provider-managed SSL

For each option, explain:
- How it works
- Cost
- Complexity
- Pros
- Cons
- Best fit for MustaFlow AI
- What infrastructure is required

Do not implement blindly before giving recommendation.

==================================================
TASK 2 — PLATFORM SUBDOMAIN PRODUCTION SETUP
==================================================

Document exactly what is needed for platform subdomains.

Example:
- PLATFORM_DOMAIN=mustaflow.app
- PLATFORM_CNAME_TARGET=hosted.mustaflow.app
- Wildcard DNS:
  Type: CNAME
  Name: *
  Value: hosted.mustaflow.app

Also document:
- Where this is set
- How to verify it
- What the expected public URL looks like
- How to test signed-out access

==================================================
TASK 3 — CUSTOM DOMAIN SSL GATE
==================================================

Until real SSL automation is implemented, add a clear gate.

Custom domain status should show:

- DNS not configured
- DNS verified
- SSL manual setup required
- SSL active
- SSL failed

If SSL is not active:
- Do not mark the custom domain as fully live.
- Do not imply it is production-ready.
- Show warning:
  “DNS is verified, but SSL certificate automation is not connected yet. This domain may not be safely available over HTTPS until SSL is configured.”

==================================================
TASK 4 — PUBLIC SLUG ROUTE CLEANUP
==================================================

Confirm the primary public route is slug-based and not ID-based.

Preferred:
- /p/:publicSlug

Avoid:
- /api/p/:projectId

If legacy ID route exists:
- Keep it internal only or remove it before production.
- Do not show ID route to users.
- Do not use raw project IDs in public URLs.

==================================================
TASK 5 — DOMAIN OWNERSHIP VERIFICATION IMPROVEMENT
==================================================

CNAME verification is good, but for better security add optional TXT verification.

Support:

TXT record:
Name: _mustaflow.<custom-domain>
Value: mustaflow-verify=<random-token>

Requirements:
- Generate verification token per project/domain.
- Show TXT instructions.
- Verify TXT record.
- Mark ownership verified only after TXT or valid CNAME passes.
- Save verification timestamp.

This prevents accidental or unauthorized domain claims.

==================================================
TASK 6 — BETTER CUSTOM DOMAIN ERROR MESSAGES
==================================================

Currently duplicate custom domain uses DB unique constraint and may return generic error.

Improve:
- If domain is already connected to another project, return clean JSON:
  {
    "error": "This domain is already connected to another project."
  }

Also handle:
- invalid hostname
- root/apex domain guidance
- CNAME missing
- wrong CNAME target
- DNS timeout
- SSL not active

==================================================
TASK 7 — FINAL PHASE 4B REPORT
==================================================

After Phase 4B, provide a report with PASS / PARTIAL / FAIL:

1. SSL strategy recommendation
2. Platform wildcard DNS documentation
3. Custom domain SSL gate
4. Public slug route cleanup
5. Domain ownership verification
6. Better duplicate-domain error
7. Better DNS error messages
8. Platform subdomain route works
9. Custom domain route works
10. Public snapshot isolation still works
11. Auth/ownership still works
12. Typecheck passes
13. API health passes
14. Unknown API route returns JSON 404

Do not publish production.
Wait for my review.