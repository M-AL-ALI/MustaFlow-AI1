Tell Replit this:

“Proceed with the full Settings + Memory parity build, but add these acceptance requirements: mobile Settings must match the Ora website settings order and behavior, not the Builder settings; Account goes first with email/name fallback using the email prefix; Change email and Change password should use Clerk inline flows when supported, with a safe fallback to website account management for social-login/passwordless accounts; Voice input must include Auto-detect mapping to null; Memory & references must include the same toggles and dependency behavior as website, especially forcing Auto-save off when Reference saved memories is off; if we require cross-device sync, do not use AsyncStorage only — store these preferences server-side so website and mobile match; Plan & usage must use website-style cards, correct renewal/cancel labels, payment method display, and website-only management buttons; do not embed Stripe checkout or billing portal natively in mobile, only open the MustaFlow website through WebBrowser with the correct www.mustaflow.com URL; Memory screen must match the Ora website Memory Center with Profile, Saved Memories, Preferences, History, Data Controls, and Project tabs; add category fields, category filters, category selector, restore superseded memories using POST /api/ora/memories/:id/restore, clear all memories, clear all conversations, rename/delete/open conversations, project tags in history, and exact website copy where applicable; wire chat requests to respect the memory/reference preferences; preserve temporary chat isolation; do not touch Talk to Ora, billing server logic, Builder features, or unrelated UI. After implementation, verify mobile tests/typecheck, website tests/typecheck, and manual QA: settings links open correct website pages, memory prefs behave correctly, user/project memories sync both ways, history opens conversations, Data Controls clear the right Ora-only data, and temporary chats never save or reference memory/history.”