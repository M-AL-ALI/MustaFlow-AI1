“This plan is directionally correct. Please proceed, but make these requirements explicit before implementation: mobile must stop using the single generic WEBSITE_SETTINGS_URL for all Ora billing actions. Each Ora Mobile billing/plan/payment button must have its own deep link: Core upgrade → /pricing?tier=core&source=mobile, Deep Wave upgrade → /pricing?tier=wave&source=mobile, Manage Ora plan → /ora/settings?section=plan&source=mobile, Change/Add payment method → /ora/settings?section=payment-method&source=mobile, Manage billing → /ora/settings?section=billing&source=mobile. Keep account email/password actions separate and do not route them to pricing or billing.
On the website, /pricing must read the tier query param after plan data loads, scroll/highlight the selected Ora plan card, and preserve the full return URL through sign-in. /ora/settings must read section=plan|payment-method|billing, wait until subscription/payment data loads, then scroll/focus the correct section. Existing checkout/portal endpoints can stay, but all return URLs must go back to Ora-specific pages, preferably /ora/settings?section=plan&subscribed=1 for subscription success, /pricing?tier=<tier>&source=mobile for pricing cancel, and /ora/settings?section=payment-method or section=billing for payment/billing portal returns.
Do not redesign billing, do not add native Stripe, RevenueCat, or in-app purchases, and do not mix Builder plan features into Ora plan cards. Add tests proving every mobile button opens the exact expected URL and no Ora billing button opens generic /settings; add website tests proving /pricing?tier=... highlights/scrolls the right card and /ora/settings?section=... targets the right section. After tests pass, this needs both website publish and a new TestFlight build, because mobile URL constants are bundled into the app.”
One small correction to Replit’s plan: the step saying “Add plan-management section to /ora/settings” should probably be “reuse/surface the existing Plan & usage limits, Payment method, and Billing sections with refs/anchors,” because those sections already exist in ora-settings.tsx. They likely don’t need to rebuild them, just deep-link and focus them properly.